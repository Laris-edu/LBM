# Phase_5 阶段状态

**最后更新**：2026-07-27
**路线决策（2026-07-22）**：用户批准 **`ROUTE_B_MAIN + 1D_REAL_AIR_BOUNDING`**（备忘录 `route_ab_decision_memo.md` 选项 1 = D0-3 预授权默认；升级条件预注册于备忘录 §5/§7）。
**G0 决策（2026-07-23，D5-2）**：用户批准 **`SCOPED_PASSED_BY_USER`**（围栏=剪切 ν 不认证 + 低波数有限-k 表格口径，见 §4）；`MODEL_CLOSURE_PASSED_ROUTE_B`。
**G3（2026-07-26）**：权威 run `20260726T082938Z` **`PASSED`**（脚本判定，七行全过；报告 `nonlinear_1d_reference_report.md`）——1D NSF 参考仪器正式认证 + 分支正式定义冻结 + p-side H2 复核闭合（备忘录 §9）。
**G1-W（2026-07-27）**：权威 run `20260727T083342Z` **`PASSED`**（八行全过；报告 `wall_nonlinearity_neutrality_report.md`）——**v1.1 对称质量中性壁=已认证生产热壁**；WP1-3 遗留 +13%/+20° 能量超额经 G0 α_eff(k) 高 k 扩展行定量归因闭合；旧壁 `PRESSURE_PRESERVING_WALL_DIAGNOSTIC_ONLY` 生效。WP1 五项 + G0 + G3 + G1-W 闭合；当前 WP2 次门 **G1a**。
**阶段名称**：Phase_5 — 热声薄膜有限温升非线性换能（Nonlinear Entry and Production Contract）
**参考合同**：`docs/Phase_5/Phase5_instruct_v1.2.md`（v1.2，2026-07-20 经 WP0 冻结为 Phase_5 唯一规范性入口与生产合同；实际入库名等同合同建议名 `Phase5_指导文档_v1.2.md`；v1.0/v1.1 未随库存档，修订历史见合同附录 C/D）
**状态口径（2026-07-20）**：**Phase_5 已立项——用户下达「开始 Phase_5」，WP0（范围与合同冻结）完成。** 合同评审基线 `b86459c590f406a2e0bfe452d6a73a61ede4b611` 与冻结时 master HEAD 一致（冻结锚点即现行代码态）。主路线=路线 B「参考态输运闭合下的全非线性理想气体」（D0-3；正式 A/B 决策待 WP1 双物性 1D 消融，默认结论 D-AB-2：`ROUTE_B_MAIN + 1D_REAL_AIR_BOUNDING`）。基础投稿目标 JASA/JSV，PRA 为证据触发的条件升级（D0-2）。基础谐波目标 H2 / 20 kHz / L2-2f（D0-8；H3/30 kHz 为 §7.4 条件项）。**全部 Phase_5 Gate `NOT_RUN`**（G5 默认 `WAIVED_JASA_SCOPE`，D0-9），生产状态 `FINAL_PRODUCTION_NOT_CLAIMED`。继承边界：M3 `SCOPED_ACCEPTED`（幅值 ±5.4% 边界、单频 10 kHz、dx2p6 不换 dx/tau，`docs/Phase_3/M3/M3_Closure_Decision.md` §3/§4 在 Phase_5 内仍有效）；M4 `PASSED_WITH_SCOPED_RISK`（非 clear PASS，digest `d69bf24d881e`）。维护基线：Phase_3 39 测试绿 + Phase_4 全量 158 测试绿。规范性优先级（合同 §0.1）：Phase 0 物理冻结 > M2/M3/M4 Closure Decision > 合同 v1.2 > 本 STATUS > Gate 报告与路线决策备忘录 > 单个算例配置/运行摘要。

## 1. 当前结论与状态标签

WP0 交付全部落地（见 §3）；**尚无任何 Gate 运行、无任何非线性物理结论**。当前状态标签（定义随合同 §0.3 冻结，本表是唯一现值追踪处）：

```text
PHASE5_NONLINEAR_ENTRY_CONTRACT_v1.2
PHASE5_SCOPE_FROZEN
ROUTE_B_MAIN
1D_REAL_AIR_BOUNDING
MODEL_CLOSURE_PASSED_ROUTE_B
NONLINEAR_WALL_NEUTRALITY_CERTIFIED
PRESSURE_PRESERVING_WALL_DIAGNOSTIC_ONLY
AMPLITUDE_ENVELOPE_NOT_CERTIFIED
HARMONIC_TRANSFER_NOT_CERTIFIED
HARMONIC_OPERATOR_ABLATION_NOT_CERTIFIED
H2_BASE_TARGET_H3_CONDITIONAL
NONLINEAR_1D_REFERENCE_CERTIFIED
DC_PROTOCOL_NOT_CERTIFIED
FINITE_WIDTH_2D_DEFERRED_JASA_SCOPE
FINAL_PRODUCTION_NOT_CLAIMED
```

> 标签备注（2026-07-22）：`ROUTE_B_DEFAULT_PENDING_WP1_EVIDENCE` → `ROUTE_B_MAIN + 1D_REAL_AIR_BOUNDING`——WP1 双物性消融触发的 `ROUTE_A_COST_REVIEW_REQUIRED` 经备忘录评审 + 用户质询验证（§3）后，**用户于 2026-07-22 批准维持 D0-3 默认**（决策记录：备忘录 §7、本文件 §4 D5-1）。升级条件预注册在案。

### Gate 状态总表（定义、Fixture 与阈值见合同 §5–§10，此处只追踪现值）

| Gate | 内容 | 当前状态 |
|---|---|---|
| G0-B | 物理模型闭合门（路线 B 有效物性；等压主路径，270–360 K） | **`SCOPED_PASSED_BY_USER`**（2026-07-23，决策 D5-2；run `20260722T173919Z`：α/c/γ@k1 +1.7%/+0.8%/+1.6% 全过。**围栏**：剪切 ν 不认证[几何无剪切+μ-only 消融零影响+多通道解耦实测；有限宽/横向流声明不可用，G5 复审触发重开]；低波数层仅按有限波数表格口径[k→0 外推不认证]。冻结文档 `nonlinear_model_freeze.md`） |
| G1-W | 热壁非线性中性与质量约束（G1a/G1b 的前置硬门） | **`PASSED`**（2026-07-27，run `20260727T083342Z`、digest `57bd724e0709`：八行全过——质量通量 1.4e-15、窗内质量 2.7e-12、u_n 6e-14、壁温 1.9e-12 K、导纳回归 +3.98%/+1.96°[能量通道 vs lbm-equivalent 谱参考]、夹具 ≤2.8e-9、旧壁 DIAGNOSTIC_ONLY、纪律过。**生产壁=v1.1 对称质量中性壁**；矩通道重标定 3.055@+17.5° 归档（§23）。报告 `wall_nonlinearity_neutrality_report.md`） |
| G1a | 规定壁温气侧幅值包络（ε_AC 至 0.05，条件 0.075/0.10） | `NOT_RUN` |
| G1b | Level C 耦合幅值包络（M3 小幅值回归为前置） | `NOT_RUN` |
| G2-T | 壁温→出射声学模态热生成链（10/20 kHz 强制） | `NOT_RUN` |
| G2-A | 纯声学传播/读出链（10/20 kHz 强制） | `NOT_RUN` |
| G2-O | 谱修正与高波数滤波谐波消融（D0-12） | `NOT_RUN` |
| G3 | 独立非线性 1D NSF 参考（双物性分支） | **`PASSED`**（2026-07-26，run `20260726T082938Z`、physics-core digest `5758666fd20d`：§8.2 七行全过——平衡精确 0、锚 +0.0014%/−0.101°@δ/12、阶 2.00、最细两档 0.130%、能量 3.6e-11、泄漏 2.0e-10+灵敏度反证、绝热 ringdown γ 比 1.004。分支正式定义冻结（`g0_measured_k1_v1`/Sutherland）+ p-side H2 复核（上界口径，备忘录 §9）。报告 `nonlinear_1d_reference_report.md`） |
| G4a | 稳态 DC 基态 + 小扰动（canonical 等温热库 `T(H_s)=T_ambient`） | `NOT_RUN` |
| G4b | 自热建立瞬态（1D 必须、LBM 条件） | `NOT_RUN` |
| G5 | 完整有限宽度二维 | `WAIVED_JASA_SCOPE`（默认，D0-9；G5-lite 可先行，非必要项） |
| 30 kHz G2 链 | H3 条件触发项（合同 §7.4） | 未触发；缺省口径 `H3_DIAGNOSTIC_ONLY` / `G2_3F_WAIVED_BY_SIGNAL` |

## 2. 合同要点（指针，不复制正文）

- **权限边界（§0.1）**：合同不覆盖/不改写 Phase 0 物理冻结、M2–M4 Closure Decision、10 kHz/dx2p6/(tau,k) 校准边界、M3 幅值边界与 M4 `PASSED_WITH_SCOPED_RISK` 真实含义、既有停放项重启条件。合同实质修改必须升级版本号并更新本 STATUS，禁止静默覆盖。
- **已冻结决策（§0.2 D0-1…D0-14）**，对执行影响最大：D0-5（A2a+QS-1 是基础论文首要物理锚；A1 `D_G>3%` 非唯一成败条件）、D0-6（`D_eng=3%` 主阈值 + 确定性 `U_gov`，不用伪统计）、D0-7（脚本只产 `SCOPED_CANDIDATE`，scoped pass 升级属用户）、D0-8（H2/L2-2f 基础目标）、D0-11（G1-W 前置硬门）、D0-12（G2-O 算子消融门）、D0-13（`H_s` 角色区分：物理热沉参数 vs 状态匹配域高检查）、D0-14（生产壁/谱修正/滤波变化=仪器变化，触发 §23 定向复验）。
- **非谈判纪律（§0.4）**：不按幅值逐点调 dx/tau/导出因子/色散因子/壁参数/远场增益；`q_feedback_relax`/拟合窗/去趋势/滤波按算例族预注册；禁止 clipping/floor/positivity repair/结果回调；G1-W 前禁把 `pressure_preserving` 壁下 DC/H2 归因气体非线性；谱修正/滤波算子栈冻结，消融只用于仪器识别。
- **驱动协议（§3）**：G0=均匀背景物性测定（等压主路径）；A1=有符号零均值热功率数值消融（非真实 Joule 协议，D0-4）；A2a=非均匀稳态 DC 基态上的增量响应（canonical 热沉=有限距离等温热库）；A2b 先 1D 后条件 LBM；A5 按 `chi_0` 设计、按 `chi_eff` 解释，大 `C_A` 点必须标 `material_relevance`。
- **QS 修正规则（§11）**：QS-0=标量工作点（代表温度冻结为 `T_bar_s`）；QS-1=完整 DC 基态线性化；判定标签 `QS0_ENGINEERING_VALID` / `QS1_BASESTATE_VALID` / `DYNAMIC_NONLINEAR_RESIDUAL_IDENTIFIED`。
- **后处理与不确定度（§12）**：多谐波联合拟合器（N=5、`x(t)=Re[x̂ e^{+iΩt}]`）；去趋势预注册（A1/A2a 主拟合=常数项）；`U_gov=max[U_det, U_95,fit]`；Go/No-Go 多信号原则（§12.5：A2a `D_OP`/QS 判别、经认证 H2、A1 `D_G`、严格上界任一即可）。
- **执行顺序（§13/§14）**：WP1 独立仪器 → WP2 入口 Gate（G0→G3→G1-W→G1a→G1b→G2-T/A/O 10/20 kHz→G4a→条件项）→ WP3 首轮 8 信息单元（≈20–40 次实际运行）Go/No-Go → WP4 完整矩阵（优先级 A2a/QS-1→A1 H2/上界→A5→少量 A3→条件 A2b/H3/F1）→ WP5 条件升级。
- **数据合同（§16）**：每 run 七文件 + 59 项 metadata + 27 项结果字段；机器可读转录=`verification/nonlinear/phase5_gate_schema.json`（冲突以合同为准）。
- **声明语言（§18）**、决策树（§19）、风险登记与止损（§21）、投稿前文献核查（§22）、版本升级与复验矩阵（§23）。

## 3. WP0 执行与验证记录

| 日期 | 动作 | 结果 | 证据 |
|---|---|---|---|
| 2026-07-20 | 用户下达「开始 Phase_5」，采纳 v1.2 为权威合同 | 立项决策 `D5-0` | 本文件 §4 |
| 2026-07-20 | 合同入库确认：`docs/Phase_5/Phase5_instruct_v1.2.md`；评审基线 `b86459c…` 与 master HEAD 一致性核对 | `FROZEN` | `git log`（HEAD=b86459c） |
| 2026-07-20 | 创建 `Phase5_STATUS.md`（本文件，含状态标签与 Gate 现值表）、`docs/Phase_5/README.md`、`Phase5_Output_Files_Guide.md` | 交付 | 本目录 |
| 2026-07-20 | 创建 gate schema（机器可读）：`verification/nonlinear/phase5_gate_schema.json` + `verification/nonlinear/` 子包（`__init__.py`、`README.md`） | 交付 | `python -c "json.load(...)"` 通过 |
| 2026-07-20 | 目录与命名规范：`configs/phase5/README.md`（合同 §17 子目录制）；更新 `configs/README.md`、`verification/README.md` 索引 | 交付 | 对应文件 |
| 2026-07-20 | 文档同步：`docs/PROJECT_CONTEXT.md` 阶段指针切至 Phase_5；`docs/Phase_4/Phase4_STATUS.md` 记录转维护态；`CLAUDE.md` 阶段状态指针更新 | 同步完成 | 对应文件 |
| 2026-07-20 | 回归 smoke：`python -m pytest verification/test_phase1_reference_data_integrity.py -q` | `passed` | 本次改动无代码行为变更（仅新增空子包与数据/文档文件） |
| 2026-07-21 | **WP1-1 交付**：`postproc/multiharmonic_fit.py`（合同 §12.1 七项输出全实现：复幅值+协方差、条件数、残差谱 FFT+定频探针、拟合窗、`PROTOCOL_DETREND` 预注册、多窗口敏感性、合成泄漏夹具；约定复用 `complex_amplitude` 单源） | 交付 | `postproc/README.md`、本表下行测试 |
| 2026-07-21 | `python -m pytest verification/nonlinear/test_phase5_multiharmonic_fit.py -q` | `11 passed` | 含 4 类非退化反例（错误去趋势偏置>1e-3 显形、20% 漂移窗口敏感>1e-2、真实 2f 污染>1e-5 被计量、域外 7f 被残差谱/探针检出且 in-basis 保持 ≤1e-8） |
| 2026-07-21 | 泄漏夹具实测（10 kHz、N=5、8 周期×64） | `max_relative_leakage=6.3e-16 ≤ 1e-8` | 合同底 8 个量级余量；target 误差 2.2e-16、条件数 1.414（=√2 解析预期） |
| 2026-07-21 | 全套收集 158→169；`test_phase1_reference_data_integrity` + `test_phase2_postprocess_modal_fit` | `10 passed` | 既有代码零改动（纯新增文件） |
| 2026-07-21 | **WP1-2 交付**：`reference/nonlinear_nsf_1d.py`（守恒 FV 中心通量 + RK4 全非线性 NSF；零质量通量固壁 + canonical 等温热库；壁温/热流(A1)/薄膜 ODE 三协议；双物性分支 T0 锚定；RK 权重机器精度质量/能量审计；四个内置仪器夹具） | 交付 | `reference/README.md`（新建）、下列各行 |
| 2026-07-21 | `python -m pytest verification/nonlinear/test_phase5_nsf1d_instrument.py -q` | `10 passed`（159 s） | 全套收集 169→179；含非退化反例（物理 2f 灵敏度、双分支发散、阻尼上界、修正优于原始） |
| 2026-07-21 | 仪器标定（toy 缩放参数，α×100/c×3）：平衡保持严格 0；ringdown 声速 −0.17%、阻尼比 1.19（bulk 下界 + 等温端层 ~+10% 预期内）；线性锚修正后 +0.02%/0.11°、箱压修正 1.83% ≈ 解析 (γ−1)δ/(√2H)=1.885%；相位误差网格二阶收敛（δ/6→δ/12：0.409°→0.111°） | 数值证据 | scratchpad 标定 log（本表即留档处） |
| 2026-07-21 | **配对泄漏夹具设计修正（方法学记录）**：首版（flux 驱动、H=10δ、settle 2）实测 2f/3f 同量级 ~1.7e-4 且反对称化不消——诊断为**慢扩散瞬态泄漏**（Neumann-Dirichlet 最慢模 τ≈H²·4/(π²α)，瞬态线性于驱动、反对称组合保留）。修正：Dirichlet 对（最慢模衰减 ×4 快）+ H=4δ + settle 7，并新增偶组合输出（瞬态消去、暴露真实物理 2f）。修正后数值底 2.25e-10 ≤1e-8、3f 1.2e-10、物理 2f 1.68e-6（≈C₂·ε 预测 1.9e-6） | 修正 `PASS` | 求解器夹具 docstring + 本行；G3 正式 run 需按此 settle 纪律预注册 |
| 2026-07-21 | **真实空气 10 kHz 线性锚点证据 run**（非 G3 gate run）：N=180（δ/12）、H=15δ=399 µm、双分支各 8 周期 | `lbm/phys 均 corr +0.001%/−0.101°` | raw −1.319%/0.634°、box 1.834%、mass ~2e-15、eres ~5e-12；**双分支线性极限简并到显示位一致**（T0 锚定按构造 Δ_prop→0）；G3 门 2%/2° 余量 >100×，正式认证（run 合同+收敛阶梯）留 WP2 |
| 2026-07-21 | **WP1-3 交付**：`boundary/wall_thermal_mass_neutral.py`（质量中性 Grad 壁候选 v1：同 Grad 重构核、ρ_w 逐列继承 post-stream 密度→壁操作质量改变按构造为零）+ `boundary/wall_mass_audit.py`（边界质量/状态审计：回调级完整记账、0f–3f 谐波分量、归一化定义随模块归档） | 交付 | `boundary/README.md` 更新；下列各行 |
| 2026-07-21 | `python -m pytest verification/nonlinear/test_phase5_wall_mass_neutral.py -q` + Phase_3 维护回归 | `8 passed`（5 s）/ `41 passed` | 全套收集 179→187；审计工具对合成已知注入校准比 1.0000；冻结路径零改动 |
| 2026-07-21 | 微型 rig 审计标定（ε=0.02、5 周期）：旧壁 `pressure_preserving` 质量源 1f=6.7e-3、2f=1.35e-4、0f=4.3e-5（2f/1f≈ε=时间导数结构）、壁归因漂移=全域漂移（审计完备+下游算子守恒）；新壁全分量 ≤2.2e-16、累计=0、u_n≤8e-17；θ/u 钉扎 ~6e-16；20 周期 ε=0.05 稳定 | 数值证据 | 微型 rig 双壁动态差异可检出但幅度是网格尺度声学 rig 伪影，不得读作生产导纳差 |
| 2026-07-21 | **生产尺度双壁判决探针**（10 kHz、dx2p6、ny=48、Level A 权威口径逐项照抄，2 周期/壁，~16 min/壁）：① 探针链验证——旧壁 `Y −5.32%/+2.198°` **逐位复现 M3 权威值**；② 旧壁生产质量源定量——`dm 1f=2.37e-6`（G1-W 门 1e-10 的 ~2.4e4 倍）、0f=4.9e-9、2f=2.9e-9、2 周期漂移 3.6e-4（合同 §6.1 风险依据坐实）；③ 新壁质量侧完美——分量 ≤1.7e-17、漂移 2.3e-13、稳定 | **④ 候选 v1 导纳预检不过**：`Y −37.9%/+2.65°`（对旧壁 0.656@+0.45°）≫ 回归门 ≤5% | **v1 不得进 G1a**（合同 §21 风险行预案："修约束重构，不用 equilibrium clamp 代替"）。三假设诊断已设计：(a) 仪器 k-窗误读→FD 双通道判别；(b) 闭箱物理→域高扫描；(c) 壁模型缺陷→row0 密度行为直读。判别结果见下行 |
| 2026-07-21 | **判别实验（4 run：grad/mn × ny48/96，FD 双通道 + 域高 + 壁密度/箱压诊断，~73 min）**：① **假设 (a) 仅仪器——否**：FD 温度梯度通道比 `mn/grad=0.500`（矩通道 0.656）——近壁温度梯度本身减半，场级差异真实（两通道比值不同提示 k-窗读数效应叠加存在但非主导）；② **假设 (b) 全局压缩主导——否**：域高 48→96 仅缓慢改善（mn −37.9%→−34.4%；grad −5.32%→−3.13%，grad 自身的高度分量顺带记录）；③ **假设 (c) 语义修正——壁局部自洽**：mn 壁 `ρ̂_row0/ρ0=7.23e-4@−164.9°`（@96：9.20e-4@−172°）≠ 开放等压值 `1.18e-3@180°`，但与**密封箱等压值**（`p̂_box/p0−θ̂/θ0`，实测 `p̂_box/p0=5.17e-4`@48、2.98e-4@96 ≈∝1/H）幅值一致——壁对其密封环境响应正确；grad 壁 ρ̂ 严格=开放等压值（拟合链 sanity ✓）且 `p̂_box~1e-6`（钳压=人为开箱） | **核心判读（见下两行更新）：两种壁在 y-周期 rig 里解的是两个不同物理问题**——旧壁的质量源与其"半空间式"导纳是同一枚硬币的两面（质量交换⇒箱压钳制⇒仿开放系）；mn 壁使 rig 成为真密封箱（无热沉、双侧层、箱压 5e-4）。**但密封箱压缩功修正上限 ~12.5%（`T̂_p/T̂_s=(γ−1)/γ·p̂/p0/θ̂·θ0`）不足以桥接 −38%→−5%**——残余 ~25% 机制未闭合（嫌疑：密封无热沉下 settle=1 周期的慢瞬态泄漏[1D 配对夹具同款教训]、双侧层相互作用、k-窗对变化场结构的读数）。**下一步实验已定**：mn 壁 + 等温顶盖（`T(top)=θ0` Dirichlet，经 `compose_boundary_callbacks` 组合）——这正是 Phase_5 canonical A2a 几何（`T(H_s)=T_ambient` 热沉，合同 §3.3），有热沉、单侧层、可与 WP1-2 1D NSF 密闭箱修正锚直接交叉验证；周期密封 rig 是 Phase_3 遗留几何、本就不是 Phase_5 生产几何 |

## 4. 决策记录（含范围对齐）

| ID | 日期 | 决策 | 决策方 |
|---|---|---|---|
| D5-0 | 2026-07-20 | **Phase_5 立项**：采纳 `Phase5_instruct_v1.2.md` 为 Phase_5 唯一规范性入口与生产合同；执行 WP0；Phase_4 转维护态（基线 158 绿 + M4 digest `d69bf24d881e`）。本记录构成 WP0 要求的**用户范围对齐记录**；导师层面对齐记录如需另行留档，由用户后续补充。 | 用户 |
| D5-0a | 2026-07-20 | WP0 落地口径：合同实际入库名 `Phase5_instruct_v1.2.md`（与合同"建议入库路径"等同，沿用仓库 `phaseN_instruction` 命名先例）；Gate schema 以机器可读 JSON 落于 `verification/nonlinear/phase5_gate_schema.json`（合同 §4/§16/§17 为规范源，JSON 为转录，冲突以合同为准并同步升级）；`configs/phase5/` 采用合同 §17 子目录制（区别于顶层扁平 configs，子目录随首个配置落地创建）；run_id 沿用 UTC 时间戳先例；G4a/G4b 结果目录族名 `g4a_dc_base`/`g4b_self_heating` 为落地约定（合同未显式冻结，schema 中已标注 convention）。 | 执行记录 |

| D5-2 | 2026-07-23 | **G0-B scoped 升级：`SCOPED_CANDIDATE` → `SCOPED_PASSED_BY_USER`**。围栏（合同 §4.3 六条件逐项满足）：① 剪切 ν 通道不认证——失败项不污染保留结论（热/声 QoI 链 α/c/γ@k1 全过；几何无剪切、WP1-5 μ-only 消融证零影响到 4 位、多通道松弛解耦经 G0 同机分测证实；Phase_3 冻结口径）；任何需要剪切的声明（有限宽度/横向流，G5 类）不可用，G5 复审时本围栏触发重开；② 低波数层按"有限波数有效系数"表格口径归档，k→0 外推不认证，下游一律按表插值。批准前用户质询"剪切与松弛的关系"并获解答（多通道解耦+实测证据）。标签 `MODEL_CLOSURE_PENDING`→`MODEL_CLOSURE_PASSED_ROUTE_B`。 | 用户 |
| D5-1 | 2026-07-22 | **路线 A/B 正式决策：批准维持 `ROUTE_B_MAIN + 1D_REAL_AIR_BOUNDING`**（备忘录选项 1 = D0-3 预授权默认）。依据：WP1 双物性消融（H2 压低 ~14×、D_OP 分支差 +2.18pp——两项均经独立验证，备忘录 §2.1）触发 `ROUTE_A_COST_REVIEW_REQUIRED`；评审结论=不启动路线 A，真实空气定量由 1D-physical 分支承载。升级条件预注册（终稿需 LBM 直接承载真实空气 H2/D_OP 定量值 → 重开代价评审）。批准前用户质询测量正确性并获验证确认。 | 用户 |

预授权口径备忘（合同已冻结、无需再决策）：路线 A 启动、`SCOPED_PASSED_BY_USER` 升级、`SCOPED_GO`/`SCOPED_GO_UPPER_BOUND`、PRA 升级与完整 G5 启动均属**用户专属决策**；脚本与报告不得自动作出。

## 5. 风险与边界

- **继承硬约束**：单频 10 kHz、dx2p6 mapping、不换 dx/dt/tau/热流导出因子/Grad 壁重构（M3 决策 §3；触发即按停放项重启流程处理）。Phase_5 新增的生产壁与算子消融要求（G1-W/G2-O）在通过前具有**阻断效力**，但不得倒改冻结上游结论。
- **热壁（G1-W 已闭账，2026-07-27）**：旧壁 `pressure_preserving` 的 O(ε)/O(ε²) 质量源在权威 run 定量归档（1f=1.0e-4@ε=0.05 vs mn 1.4e-15）→ `DIAGNOSTIC_ONLY` 标签生效；**生产壁=v1.1 对称质量中性壁（已认证，非 equilibrium clamp、保留物理 neq 热流）**。等温盖几何在冻结栈判死的约束仍在（canonical 盖几何认证留 G4a 处理）。
- **【已闭合】质量中性壁能量通道 +13%/+20° 超额（2026-07-26/27 归因闭合）**：预注册路径执行完毕——G0 仪器高 k 扩展行实测 α_eff/α_nom=1.58–12.5×（k=0.26–1.05）→ lbm-equivalent 密封谱参考 1.0087@+21.09°（常 α 验证=三方验证闭式值逐位）→ 实测能量通道 1.0488@+23.05° 回归残差 +3.98%/+1.96°（门内）。机理：密封箱响应带横跨冻结栈 α_eff(k) 强非单调区，名义 α 连续介质参考在此仪器上不成立；缝×FFT 稳态谐波注入底板实测 ≤~6e-10（G2 底板输入）。矩通道场形重标定常数 3.055@+17.5° 归档（§23 换壁重标；G1a/G2 矩通道读出定标用）。详见 `wall_nonlinearity_neutrality_report.md` §2/§3。
- **Hermite 温度窗风险**：若 `epsilon_AC=0.03` 已失稳，停大信号主线，转 A2a 小扰动/上界/方法路线（§21）；G1a 最低幅值窗=0.05，未过则不授权完整生产矩阵。
- **算子消融风险**：G2-O 若显示谱修正/滤波开关改变谐波符号、阈值或阶次，谐波降 L1/诊断；禁止按结果挑"更好看"的算子栈。
- **规划非承诺**：合同附录 E 工期为非规范性估计；首轮 8 信息单元 ≈ 20–40 次实际运行（§14），生产物理点与验证/网格/窗口/空检/消融点必须分别预算；禁用"40 算例两周"式高置信承诺。
- **`results/phase5/` 不入库**（`.gitignore` 沿用）；权威 run 精选摘要归档约定见 `Phase5_Output_Files_Guide.md`。

## 6. 下一步（WP1 独立仪器开发）

按合同 §13 WP1 交付清单执行（建议顺序按依赖排列，多谐波拟合器最先——1D 门与后续全部 Gate 都消费它）：

1. ✅ `postproc/multiharmonic_fit.py` — **已交付（2026-07-21）**：§12.1 七项必需输出全实现、§12.2 `PROTOCOL_DETREND` 预注册、复幅值约定复用 `phase3_interfaces/complex_amplitude.py` 单源；11 项仪器测试绿（含非退化反例），泄漏夹具实测 6.3e-16 ≤ 合同底 1e-8。
2. ✅ `reference/nonlinear_nsf_1d.py` — **已交付（2026-07-21）**：全非线性守恒 NSF（FV 中心 + RK4 低耗散）、三协议（壁温/A1 热流/薄膜 ODE）、双物性分支 T0 锚定、零质量通量固壁、内置平衡/锚点/ringdown/泄漏夹具；10 项仪器测试绿。仪器级已对齐 §8.2 各门方向（平衡严格 0、真实空气锚 0.001%/0.101°、泄漏底 2.3e-10、审计机器精度）；**G3 正式认证（run 合同七文件 + ≥1.5 收敛阶梯 + 报告）留 WP2**。
3. ✅ **仪器层面完整（2026-07-22）**：质量中性热壁候选 + 边界通量审计（§6.1）——审计工具（双尺度校准）、免标定能量平衡读出（1D 自校验 0.9990）、密封连续介质参考三方交叉验证（BVP=闭式=1D）、候选 v1（单侧，供盖几何）+ v1.1（对称双侧，供密封 rig，对称比 1.0000）质量/钉扎/稳定全机器级、10 项仪器测试绿。v1 导纳之谜完全分解；剩余 +13%/+20° 能量超额**路由到 G0**（α_eff(k) 箱尺度行为）；G1-W 认证路径预注册（G0 → §23 矩导出重标定 → 密封几何回归夹具）。等温盖几何判死与"旧壁导纳=质量源同硬币"机制为 G1-W 报告核心素材。
4. ✅ 低马赫与"边界—线性内部"夹具（2026-07-22）：1D ringdown/低马赫（WP1-2）+ `signed_pair_combination`（通用符号对，§6.1 夹具落地）+ LBM 静 rig 纪律（箱共振反例固化）。
5. ✅ WP1 双物性 1D 消融 + `route_ab_decision_memo.md`（2026-07-22）：**`ROUTE_A_COST_REVIEW_REQUIRED` 成立（H2 压低 14–23×、D_OP 符号翻转）**；备忘录建议维持 D0-3 默认（路线 B 主线 + 1D 定界）+ 预注册升级条件；**路线决策待用户（备忘录 §7）**。

**WP1 全部五项完成（2026-07-22）；WP2 已闭合 G0（scoped by user，D5-2）、G3（PASSED，2026-07-26）、G1-W（PASSED，2026-07-27）**。下一步：**G1a**（规定壁温气侧幅值包络，合同 §6.2：G1-W 认证生产壁 v1.1、ε 阶梯 0.001→0.05+条件 0.075/0.10、基准网格+一档诊断细化、冻结谱修正/滤波栈）→ G1b（Level C 耦合包络 + 生产壁小幅值 M3 回归重跑）→ G2-T/G2-A/G2-O（10/20 kHz）→ G4a →（条件项）。G1a 可直接消费的已认证仪器：v1.1 生产壁 + 能量通道读出 + 矩通道重标定常数（3.055@+17.5°）+ lbm-equivalent 谱参考 + 夹具底板 ≤6e-10。

## 7. 更新日志

| 日期 | 更新 |
|---|---|
| 2026-07-20 | **Phase_5 立项、WP0 全部交付**：合同 v1.2 入库确认（评审基线=HEAD `b86459c`）；创建本 STATUS（状态标签 + Gate 现值表全 `NOT_RUN`）、`README.md`、`Phase5_Output_Files_Guide.md`、gate schema `verification/nonlinear/phase5_gate_schema.json`、`configs/phase5/README.md`；更新 `configs/README.md`、`verification/README.md`、`PROJECT_CONTEXT.md`（阶段指针）、`Phase4_STATUS.md`（转维护态）、`CLAUDE.md`（阶段状态指针）。下一步 WP1。 |
| 2026-07-23 | **G0-B 用户 scoped 升级（D5-2）**：`SCOPED_CANDIDATE`→`SCOPED_PASSED_BY_USER`；围栏=剪切 ν 不认证（需剪切的声明不可用、G5 复审触发重开）+ 低波数有限-k 表格口径；标签 `MODEL_CLOSURE_PASSED_ROUTE_B`。批准前用户质询"剪切与松弛"获多通道解耦+实测证据解答（决策留痕 §4）。run 归档中脚本判定不改。**WP2 首门闭合，当前 G3**。 |
| 2026-07-27 | **G1-W 权威认证 PASSED（WP2 第三门闭合，两次权威 run ~6+7.5 h + Stage-1 归因 + 夹具重设计）**：runner `scripts/phase5_g1w_wall_neutrality.py` + 配置 + 合同测试 4 绿（含谱参考对独立闭式解的非重言锚）。**Stage-1 归因闭合（关键路径前置）**：WP1-3 遗留 +13%/+20° 能量超额的主嫌疑（α_eff(kbox) 单点）被谱计算否证（方向相反）→ 合成扫描给出可测预言（表外 α 需 3–5× 名义）→ **G0 仪器补测高 k 行实测 1.58–12.5×（k=0.26–1.05，非外推）** → lbm-equivalent 密封谱参考 1.0087@+21.09°（常 α 验证=三方验证值逐位复现）。**首次权威尝试 `20260727T040121Z`**：7/8 行过（能量通道 1.0488@+23.05° 复现 WP1-3、回归 +3.98%/+1.96° 门内、profile rms 3.2%、旧壁 DIAGNOSTIC_ONLY）、夹具行 FAIL（底板 ~1e-7）→ **零成本窗口判别**：2f=瞬态（τ=1.1 周期实测）、3f=稳态→ε² 标度检验支持真实三次物理（C₃≈0.28）→ 夹具重设计（ε 1e-3→1e-4 + settle 8→12，预注册）→ **权威 run `20260727T083342Z` PASSED**：夹具 2f=3.3e-10、3f=2.77e-9（**恰 ×100=ε² 标度实测验证**）；八行全过。**生产壁=v1.1 对称质量中性壁**；矩通道重标定 3.055@+17.5°（与 WP1-3 的 0.328@−17.9° 精确互为倒数）；冻结栈稳态谐波注入底板 ≤~6e-10 实测界定（G2 输入）；旧壁链验证 vs M3 +0.18%/+0.10°。报告交付 + M5_runs 归档。**下一门 G1a** |
| 2026-07-26 | **G3 权威认证 PASSED（WP2 次门闭合，两次权威 run ~2×66 min + 仪器修正）**：runner `scripts/phase5_g3_nsf1d_reference.py`（七文件 run 合同 + §16.2/16.3 全字段）+ 合同测试 5 绿 + `g0_measured_transport()` 正式分支构造器。**首次尝试 `20260726T074420Z` FAILED**——唯一失败行 ringdown γ 比 14.48，定量归因**欠分辨等温端热沉伪影**（δ_κ(347 kHz)=0.14 µm ≪ dy=7.8 µm，无缓冲热沉 2k/dy；解析 ~9.1e3/s vs 实测超额 7.94e3/s；ν×100 仪器变体被物理阻尼掩蔽）→ fixture 重设计**密封绝热箱**（mode-1 真离散本征模）→ **权威 run `20260726T082938Z` PASSED**：γ 比 1.004、等温诊断双点超额 7937→4525/s（比值 0.570≈∝1/dy 预测 0.5，与格式耗散反向=归因坐实）；非 ringdown 块两 run 逐位一致。其余六行：平衡精确 0、锚 +0.0014%/−0.101°、阶 2.00/最细两档 0.130%、能量 3.6e-11、泄漏 2.0e-10+灵敏度 5.7e-7、可分辨性 q 1.6e5。**预注册复核闭合**：p-side H2 贴底板（H3p≈0.6·H2p、无 ε² 标度）→ 上界口径 `H2p≤2.1e-5` + T-side 结论不改（备忘录 §9）；A1 底板 2f=3.15e-5（U_det 输入）；D_G 于正式 g0 分支 4.0e-5（`>3%` 不可达坐实）。报告交付 + M5_runs 归档。**下一门 G1-W** |
| 2026-07-23 | **G0 实测律挂接复核（预注册触发，~13 min）**：`1D-lbm-equivalent_g0_measured_k1_v1`（k_eff∝T^1.04、μ∝T^−0.60）重跑 WP1-5 全协议、三分支对照。**结果**：g0 分支 H2 自身压低 9×（两分支反相分居相消零点两侧 +65°/−50°）、H2 判据比 3.1→**1.1-1.2**（生产 ε 擦线）；**Δ_prop(D_OP) +2.18pp→−0.38pp 恰在判据线上**（符号翻转消失；g0 D_OP +1.25% vs 密封闭式 +1.34%，交叉验证 0.09pp）。**D5-1 路线决策追溯性强化**——路线 B 的 LBM 在标定点内禀携带近空气导热标度。备忘录 §8 追记；caveat：k1 单点律 surrogate，分支正式定义 + p-side H2 归 G3 | 复核闭环 | 下一步 G3（WP2 次门） |
| 2026-07-23 | **G0-B 实现 + 权威 run（WP2 首门）**：三个 P2 测量 helper 加背景态扩展（默认逐位不变，P2 回归 17 绿）；runner `scripts/phase5_g0_effective_properties.py` + 首个 phase5 子目录配置 + 合同测试（3 绿）；mini smoke 抓出并修正两处（CSV 字段、**门径校准点**——低波数层不收敛于名义值反发散 +44%@k=0.049，300 K 回归改锚 (tau,k) 标定点 k1）。**权威 run `20260722T173919Z`（31 行，digest `8bbacf242674`，~66 min）verdict=`SCOPED_CANDIDATE`**：α@k1 +1.73%✅ c +0.82%✅ γ +1.64%✅ 窗口 0.01%✅；scoped=ν@k1 −33.8%（Phase_3 非 QoI）+ 低波数收敛 23.2%（有限-k 口径）。**有效律冻结（表格口径，无单一幂律）**：α_eff 温度指数随 k 从 1.04（k→0，k_eff≈常数）到 2.04（**k1：k_eff∝T^1.04，与真实空气 T^0.89 仅差 0.15！**）到 5.4（k2 谷底 α=0.48×名义）；等压/等密度 LU 逐位简并（运动学系数密度无关）；x/y 逐位各向同性；c/√(γθ_b) 全温度 +0.7~0.9% | 冻结文档 `nonlinear_model_freeze.md` 交付；精选摘要归档 `M5_runs/g0_20260722T173919Z/`；三个下游输出：①1D-lbm-equivalent 挂 T^1.04 实测律（预注册复核——预期两 1D 分支近重合、Δ_prop 大幅收窄、路线 B 追溯加强）②WP1-3 归因输入（kbox α=0.60×名义）③G2 各谐波按各自 k 有效物性传播 | **G0 scoped 升级决策属用户** |
| 2026-07-22 | **路线 A/B 正式决策（D5-1）**：消融结果经用户质询触发的独立验证（D_OP 闭式渗透率 0.1pp、H2 指数扫描穿零+相位翻转）确认后，**用户批准维持 `ROUTE_B_MAIN + 1D_REAL_AIR_BOUNDING`**。标签更新（§1）、备忘录 §7 决策记录、升级条件预注册生效。WP1 就此全面收官（五项交付 + 路线决策闭环）；进入 WP2 G0。 |
| 2026-07-21 | **WP1-1 多谐波联合拟合器交付**：`postproc/multiharmonic_fit.py`（§12.1 七项输出 + §12.2 预注册表；约定复用 `complex_amplitude` 单源、相位参考调用方时钟）+ `verification/nonlinear/test_phase5_multiharmonic_fit.py`（11 绿，含错误去趋势/漂移/真实 2f/域外 7f 四类非退化反例）；泄漏夹具实测 6.3e-16 ≤ 合同底 1e-8；全套收集 158→169、既有代码零改动。新增 `postproc/README.md`。下一项 WP1-2 非线性 1D NSF 双物性求解器。 |
| 2026-07-21 | **WP1-2 非线性 1D NSF 双物性求解器交付**：`reference/nonlinear_nsf_1d.py`（守恒 FV+RK4、三协议、双分支 T0 锚定、机器精度审计、四夹具）+ `verification/nonlinear/test_phase5_nsf1d_instrument.py`（10 绿，~2.5 min；全套收集 169→179）。数值证据：平衡保持严格 0；声速 −0.17%；toy 锚 +0.02%/0.11°（箱压修正 1.83%≈解析 1.885%）；**真实空气 10 kHz 锚 corr +0.001%/−0.101°（双分支简并）**；泄漏数值底 2.25e-10 + 物理 2f 灵敏度 1.68e-6；双分支 ε=0.2 发散反例 2f 差 61%。配对夹具慢瞬态泄漏诊断与设计修正留档（§3）。新建 `reference/README.md`。下一项 WP1-3 质量中性热壁候选 + 边界通量审计。 |
| 2026-07-22 | **canonical 盖几何判决 + 瞬态假设否证（3 run + 1D 匹配锚，~80 min）**：① 新增质量中性等温顶盖 `make_top_mass_neutral_lid_callback`（canonical A2a 热沉的实现，组合 smoke 质量漂移 4e-16，测试 9 绿）；② **等温盖 rig 在冻结栈上判死**——mn+盖与 grad+盖读数完全相同的巨大伪影（`Y +1204%/+139°`，ratio 1.0067@0.37°；FD 通道同飙）：第二条边界行触发 **P4-1 同款"全局周期 FFT 修正 × 边界缝"注入并在两边界行间的有界腔内累积**（P4-1 有界柱 `\|R\|≈0.2–0.3` 结论重现；密封单缝 rig 无此腔）——G1-W 夹具设计硬约束：canonical 几何导纳回归不能在冻结谱修正栈上直跑；③ **瞬态泄漏假设否证**：密封 rig settle 1→3 周期读数不动（−37.86%→−37.81%），去趋势 0/1 双拟合逐位一致；④ **压缩功修正后 `Ycorr=−29.7%/−0.29°`**（用实测复 `p̂_box/p0=5.18e-4@−21.7°`）——相位干净、纯幅度亏损 ~0.70×；⑤ 1D 匹配几何锚（等温盖、H=4.61δ、N=47）完美：raw −4.09%/+2.15° → corr +0.05%/−0.09° | 幅度亏损 ~0.70× 且相位干净 → 主嫌疑收敛为**读出链失准**（矩导出与 FD 均按 grad 壁场形 (tau,k) 点标定；密封连续介质参照[双侧驱动]解析估计仅 −8%）vs 真实欠传导 | **能量平衡判别已设计并启动**：密封箱 `q̂_net=iΩ·(cv/R)·H·p̂_box` 只用精确守恒矩（免输运标定）；1D 求解器新增 `lid_bc="adiabatic"`（密封对称面精确类比，第 11 项仪器测试绿）作连续介质参照 + 能量读出法在 1D 精确通量上自校验 |
| 2026-07-22 | **能量平衡判别 + 三方仲裁（~40 min LBM + 独立 BVP 裁判）**：① 免标定读出法（密封箱 `q̂_net=iΩ·(cv/R)·H·p̂_box`，只用精确守恒矩）在 1D 精确通量上自校验 `R=0.9990@0.00°` ✓；② **密封-绝热连续介质参考三方交叉验证**：独立复系数 BVP 有限差分 = 解析闭式 `q̂=km(T̂_s−T̂_p)tanh(mL)`（`T̂_p` 密封闭包）= 非线性 1D 求解器——L=2.356δ 处 `Y/Y_hs=0.9202@+3.53°`（即 −8.0%）三方逐位一致，L=6δ/15δ 单调趋近半空间 ✓（**诚实记录**：判别探针首打的"1D −92.2%"是探针自身归一化 bug——夹具 0.03 K 驱动的响应误除以 0.354 K，0.92×0.0847=0.078 完全对账；非求解器问题）；③ **LBM mn 密封 rig 判决（修正参考后）**：能量读出净通量 `1033@+68°` = 连续介质密封净值 `911@+48°` 的 **1.13×@+20°**；矩通道净值 `339@+50°` = 0.37×；**矩/能量 = 0.328@−17.9° → 矩通道在 mn 壁场形上失准 3× 实锤**（handoff 与 row-fit 一致性 1.0000 ✓ 提取链本身自洽）；④ **v1 结构缺陷暴露**：顶/底侧矩通量 `35@75°` vs `308@48°`——neq 仅从 row1 复制使 wrap 背面不是合格的壁，密封 rig 永远测"壁+畸形镜像"、无法单壁归因（能量读数的 +13%/+20° 超额即含背面与缝注入贡献） | **v1.1 设计已定：对称双侧壁**——按方向分侧构造 neq（`cy>0` 用 row1、`cy<0` 用 row ny−1、`cy=0` 取均），使密封 rig 成为真双侧驱动箱，与三方验证的连续介质参考直接对标（能量+矩双通道）；若能量净值落 `2×0.92·Y·T̂` 数 % 内→壁物理认证成立，矩通道读数因子成为 G1-W 的 (tau,k) 重标定输入（合同 §23 换壁重标，预注册、非结果调参） | 质量中性性、θ/u 钉扎、稳定性全程保持机器级 ✓ |
| 2026-07-22 | **v1.1 对称双侧壁交付 + 判决（~38 min）**：`make_symmetric_mass_neutral_wall_callback`（按方向分侧 neq：`cy>0`→row1、`cy<0`→row ny−1、切向取均；混合矩残余用**平衡增量减除**精确清零——纯平衡内容无 ghost）；仪器测试 10 绿（钉扎 4e-15、u 1.7e-14、逐回调质量 2e-16、与 v1 wrap 侧场级差异可检出）。**判决**：① 对称性完美恢复 `q_top/q_bot=1.0000@0.00°`（169.6@50.4° 双侧）；② **不变性**——净矩通量与能量读数与 v1 逐位相同（对称化重分配两侧、不改全局记账）；③ 剩余缺口收敛为单一开放量：能量通道逐侧 `Y/Y_hs=1.044@+23.3°` vs 三方验证真值 `0.9202@+3.53°`（+13%/+20° 超额） | **归因路由到 G0**：密封箱响应含箱尺度模（k=2π/48≈0.131，恰在传导导出窗 1.518 驼峰），α_eff(k) 在标定点外的行为正是 G0-B 的测量对象（低波数层 + 生产波数层）；缝×FFT 注入能量为次级嫌疑。**WP1-3 仪器层面就此完整**——继续归因需要 G0 数据，合同 WP2 顺序（G0→G1-W）被证据链反向确认 | 矩/能量 0.328@−17.9° 在干净对称壁上复现 → (tau,k) 重标定输入确认 |
| 2026-07-22 | **WP1-4 交付**：`postproc/multiharmonic_fit.py` 新增 `signed_pair_combination`（通用符号对组合——合同 §6.1"边界—线性内部"夹具在不可线性化栈上的落地；奇组合杀偶阶=数值底、偶组合杀线性瞬态=物理 2f）；LBM 静 rig 纪律固化进测试（**箱共振反例**：32 步/周期 rig 箱模 ~34 步近共振→幅值无关余震底 2.2e-3；128 步/周期 + 缓 ramp + settle 14 → 底 4.5e-8 且随 settle 衰减；生产频率箱模分离 ~1500×，1e-8 门在 G1-W 正式 run 断言）。1D ringdown（WP1-2）+ 本件 = WP1 低马赫/边界夹具清单闭合。测试 22 绿 | 交付 | `test_phase5_wall_mass_neutral.py` 第 11 项（含共振反例 >100× 判别力断言） |
| 2026-07-22 | **WP1-5 双物性消融 + 路线备忘录交付（真实空气 10 kHz，12 run ~31 min）**：A1 阶梯（ε=0.005–0.10 ×2 分支）+ 敏感性（网格/窗）+ A2a-lite（规定 `Θ_DC=0.05` 壁均值，状态匹配按构造）。**结果**：① 线性简并完美（ε=0.005 双分支 G1 前 6 位一致）；② **D_G 通道**：两分支至 ε=0.10 线性到 U_det（lbm +3.7e-5/phys +1.1e-5）——A1 `D_G>3%` 该协议下不可达（与 D0-5 预判一致）；③ **H2**：lbm m₂≈2 干净标度（H2/ε≈0.13）、**物理分支压低 14–23×**（k(T) 调制与 EOS 反号相消）→ 5/5 点违反 D-AB-2；④ H3 双分支低于底板（≤3e-5，上界口径）；⑤ **D_OP 符号翻转**：lbm −1.31%（密度通道）vs phys **+0.87%**（导热率压倒）——违反 D-AB-2 最强形式（改变主结论符号）；QS-0 分支内均有效（误差 ~8–11% of effect） | **`ROUTE_A_COST_REVIEW_REQUIRED` 成立（H2+D_OP）**；备忘录 `route_ab_decision_memo.md` 交付（建议维持 D0-3 默认 `ROUTE_B_MAIN + 1D_REAL_AIR_BOUNDING` + 预注册升级条件；决策属用户） | H2 为 T-side 口径（p-side 消融归 G3/WP2 复核）；`1D-lbm-equivalent` 待 G0 实测律挂接后复核（备忘录 §6） |
| 2026-07-22 | **消融结果独立验证（用户质询触发，~10 min）**：① D_OP 闭式热渗透率效验——双分支钉到 0.1pp、分支差闭式 2.13pp vs 实测 2.18pp；**表述修正**：密封箱 DC 压升共同贡献 +1.24pp，恒压等效 lbm −2.41% vs phys −0.28%（近零、`ρk∝T^{k_exp−1}` 相消，合同 §2.2 同族）——"符号翻转"是密封几何叠加、几何无关结论为"常物性明显衰减 vs 真实空气近不变"；② H2 k-指数扫描（0→1.2 五点 + μ-only 对照 + 长 settle）——H2 平滑单调降、空气指数略上方穿越近零、越过后相位反转 168°（参数化相消指纹）、μ 通道贡献恰为零（四位一致）、Sutherland↔幂律跨形式一致 3 位；底板 caveat：小 ε 点污染，可靠压低倍数 ~14×（ε≥0.03） | 两指标测量**均验证正确**；备忘录 §2.1 已增补、§3 判据行已精化（判定不变） | 统一图景：空气 k 指数 ≈0.9≈1 使 k(T) 效应在 DC 与 2f 通道同时近抵消密度/EOS 效应——两发现同源 |
| 2026-07-21 | **WP1-3 部分交付 + 关键发现**：审计工具 `boundary/wall_mass_audit.py` 交付（合成注入校准 1.0000；G1-W 风险项双尺度定量：旧壁生产 1f=2.37e-6 ≫ 门 1e-10）；质量中性壁候选 v1 `boundary/wall_thermal_mass_neutral.py` 交付（质量/钉扎/稳定机器完美）+ 8 项测试绿（全套 179→187、Phase_3 回归 41 绿）。**判决探针（Level A 口径逐位复现 M3 后可信）显示 v1 导纳幅值 −37.9%（相位 +2.65°）不过 ≤5% 回归门→不得进 G1a**；三假设诊断（矩导出 k-窗误读 / 闭箱双侧层物理 / 壁模型缺陷）+ FD 双通道判别 + 域高扫描已设计（§3/§5）。下一步=判别实验→v2 约束重构。 |
| 2026-07-22 | **WP1-3 仪器层面收官（四轮判别链闭环）**：判别→盖几何判死+瞬态否证→能量平衡判别（+探针归一化 bug 自纠）+BVP 三方仲裁→v1.1 对称双侧壁交付并判决。最终图景：v1 导纳之谜 −37.9% = 密封物理 −8%（三方验证）⊕ 矩通道场形失准 ~3×（免标定能量读出实锤，在干净对称壁上复现 0.328@−17.9°）⊕ wrap 畸形（v1.1 修复，对称比 1.0000@0.00°）；剩余 +13%/+20° 能量超额路由到 **G0 α_eff(k)**（箱尺度 k≈0.131 在导出窗驼峰处；合同 G0→G1-W 顺序获证据链反向确认）。新增交付：v1.1 对称壁、质量中性等温顶盖、`lid_bc="adiabatic"`（1D）、能量平衡读出法、密封参考三方验证。测试 10（壁）+11（1D）绿、全套收集 189。G1-W 认证路径预注册（§5）。下一项 WP1-4/WP1-5。 |
