# M4 验证报告 — D3 单向多域架构端到端远场（P4-5/P4-6）

**日期**：2026-07-09
**M4 gate**：**`PASSED_WITH_SCOPED_RISK`**（合同 §10.4 措辞；硬门 1–4 全过 + 预算分解交付；scoped 风险见 §7——**非** clear PASS、**非** final production pass、不授权频扫）
**架构**：P4-D3 单向多域（立项 `P4_D3_Multidomain_Acoustic_Project.md`，P4-1 单网格开边界 FAILED 后经用户决策的绕行路线；E1 的"开顶细域近场采集"由 **compact-source handoff** 替代——架构口径变更已在立项 §11 记录并经用户批准）。
**权威 run**：`results/m4/20260709T121241Z/summary.json`，physics-core digest **`cbcf7d738ede…`**；E2 门测试 `verification/test_phase4_m4_endtoend.py`。

---

## 1. M3 继承边界声明（合同 §11.2-1）

- 源数据 = Level C canonical run `T̂_s = 0.37269 K @ −47.535°`（`results/m3/20260702T114339Z`，digest `26be2fde3cc2…`）。
- M3 终态 = **`PHASE_PASS_AMPLITUDE_BOUNDARY`（相位达标、幅值边界 ±5.4%）、非 clear PASS**；本报告一切绝对幅值/SPL 声明**携带 ±5.4% 幅值带**（对应 SPL ±0.46 dB）。
- 范围继承：**单频 10 kHz**、dx2p6 热区标定、紧致空气薄膜目标；M4 通过 ≠ final production pass；频扫/功率扫属 Phase_5。

## 2. 开边界验证（合同 §11.2-2）

D3 路线下开边界在**粗声学域**（无 dispersion → 无 P4-1 体积注入底板）：

| 项 | 值 | 证据 |
|---|---|---|
| 顶开边界（80 行扰动衰减 sponge）`\|R\|` | **0.0004**（≪ 门 0.05） | 立项 §9，脉冲特征反射计，非退化（刚性盖对照 1.26、thickness 单调 0.066→0.0004） |
| 单向注入边界（底）下行脉冲 `\|R\|` | **0.0010**；刚性底对照 0.80 | 立项 §11（G-D3-3 one-way PASS） |
| 本 run 链路诊断 | 单向 `w⁻/w⁺`≈0.011、带平坦 ~0.2%、质量漂移 ~1e-6 | E2 summary `chain_diagnostics` |
| **气侧 CV 通量审计（定量）** | `I(y)=½Re[p̂v̂*]`：I_start=4.496e-4 W/m²、带内闭合 ~1%（倾斜 −1.08% 与 1.1% 单向性干涉一致）、平坦 0.32% | E2 summary `cv_audit`（`GAS_CV_AUDIT_DIAGNOSTIC_QUANTIFIED`） |

测量方法学：P4-1 确立的特征分解反射计（`Â±=(p̂±Z₀v̂)/2`）；~0.011 的单向读数底 = 校准介质下 LU-Z₀ 读出的阻抗失配残差（立项 §12.3 注），非真实反射。

## 3. 控制面 schema（合同 §11.2-3）

- **位置**：校准粗域 band row 246（primary）/ 318（R2 对照）；G 参考面 row 150；源行 y_s=90；均在底吸收带（60 行）与顶 sponge（80 行）之外的干净行波区。
- **量与约定**：x 平均 `(p̂, v̂_y)` 复幅值（`x(t)=Re[x̂e^{+iΩt}]`，P3-0 冻结），trailing 3/10 周期 LS 拟合（`phase3_interfaces/complex_amplitude`）。
- **导数双通道**（合同 §6）：primary = 动量通道 `∂p̂/∂n=−iΩρ₀v̂_n`（`farfield/dpdn_from_velocity`）；cross-check = 出射假设 `−ik p̂`。**本 run 通道差 −0.36% / −0.07°**（阈 10%/10°，余量 28×）。
- 约定 fixture：K0 fixture-3（平面波经速度通道，0.49%/0.50°）即合同 C0 的等价认证。

## 4. Kirchhoff kernel 验证（合同 §11.2-4）

`farfield/kirchhoff_2d.py`，**K0 PASSED**（立项 §12.4；`verification/test_phase4_kirchhoff.py` 4 绿）：

- **约定一次钉死**：`e^{+iΩt}` 下出射 Green = `(−i/4)H₀^{(2)}(kR)`（计划书 `hankel1_2d_outgoing` 简写的本约定映射）；prefactor 冻结、**未在端到端反调**（本报告链条中 kernel 参数零改动）。
- fixture：圆柱波 0.082%/0.034°（含离轴 5 观察点）；离散收敛（1.5/λ 欠采样错 164% → 12/λ 截断底板 ~0.06%）；速度通道 0.49%/0.50°；**错核反例 104%**（判别力）。
- 本 run 使用：600λ 孔径、12 采样/λ、x-uniform tiling（x 周期法向出射范围的精确构造）。

## 5. 端到端远场结果（合同 §11.2-5）

**链**（全部常数上游冻结，run 内零调参）：`T̂_s`(M3 canonical) → compact-source 映射（§12.1，on-stack MAP CHECK 1.001）→ `u_src=1.4683e-3 m/s` → `dp̂_phys=0.5997 Pa`（每侧）→ **G 预补偿注入**（G=0.1580@+152.4°，§12.3 冻结于任何远场之前；盲用重现 handoff **+1.18%/+0.12°**）→ 校准粗域（c_SI +0.17%）→ 控制面 → K0 kernel → 远场。

**参考 = R1（compact thermophone 平面波公式）**：`p̂_R1(y)=dp̂_phys·e^{−ik_air(y−y_ref)}`。

| 观察点（控制面上方） | `\|p̂\|` | SPL | 幅值误差 | 相位误差 |
|---|---|---|---|---|
| +20λ（y=0.776 m） | 0.6045 Pa | **86.60 dB** | **+0.80%** | +0.13° |
| +50λ（y=1.817 m） | 0.6097 Pa | 86.67 dB | +1.66% | +1.21° |
| +100λ（y=3.552 m） | 0.6134 Pa | 86.72 dB | **+2.28%** | +1.07° |

**E2 门（合同硬门 4）：幅值误差 max 2.28% < 10% ✓（4.4× 余量）**；相位 max 1.21° < 10° ✓；**R2** 控制面位置自洽 0.18% < 5% ✓。误差随高度微增 = Kirchhoff 孔径截断（与 K0 fixture 截断底板定标一致）。

**绝对声明（携带 M3 带）**：法向远场 `p̂ ≈ 0.60 Pa`（**86.6 dB SPL ± 0.46 dB**[M3 ±5.4%] ± 源实现 ~±5%），10 kHz、每侧、x 周期法向出射范围。

## 6. 误差预算（合同 §11.2-6 / §10.4-5）

| 预算项 | 量级 | 锚 |
|---|---|---|
| M3 继承幅值带（绝对声明用，E2 比值不含） | ±5.4% | M3 决策/canonical digest |
| 源映射 on-stack 实现（绝对声明用） | **1.0006 ± ~3%**（y0 原点免疫，§12.1.1 扫描证明；相位 +5.335°=真实栈↔映射偏移、只进绝对相位声明） | 立项 §12.1/§12.1.1 |
| 气侧 CV 通量闭合（粗域，定量诊断） | ~1%（带内；与单向性干涉一致） | 本 run `cv_audit` |
| 注入 G（盲用重现，E2 内实测） | **+1.2%** | 本 run handoff plane |
| 开边界反射 | <0.1%（`\|R\|`=0.0004） | 立项 §9 |
| 声速标定 | 幅值 ~0；相位 ~1–2°/传播程 | 立项 §12.3（c_SI +0.17%） |
| Kirchhoff 离散/截断 | ~0.5–2%（随观察高度） | §12.4 fixture + 本 run 高度趋势 |
| 控制面导数通道 | −0.36%/−0.07°（实测） | 本 run |
| **E2 合计（实测）** | **2.28% / 1.21°** | `cbcf7d738ede` |

分解自洽：E2 ≈ G 重现 1.2% ⊕ 截断 ~1% ⊕ 读出 ~0.3%。绝对 SPL 声明总带 ≈ ±5.4% ⊕ ±3% ⊕ 2.3% ≈ **±7%**（(b) 清偿后收紧，原 ±8%；合同 `<10%` 预算内）。

## 7. 剩余风险与 Phase_5 入口（合同 §11.2-7）

**Scoped 风险（`PASSED_WITH_SCOPED_RISK` 的构成；#2/#3 已按收尾决策 (b) 清偿，2026-07-09）**：
1. **架构替代**（保留，声明性）：合同 E1 原文（开顶细域近场采集）被 D3 compact-source handoff 替代（P4-1 FAILED 的用户批准绕行）；细域"辐射提取"已判死（立项 §12 注入淹没 31–57×）——远场结论依赖 compact-source 映射的 on-stack 锚（§12.1）而非细域直接辐射。
2. **气侧 CV 审计 → `DIAGNOSTIC_QUANTIFIED`（已清偿）**：粗域声能通量审计落地——带内闭合 ~1%（I_start=4.496e-4 W/m²，−1.08% 倾斜与 1.1% 单向性干涉一致，平坦 0.32%），无未解释增/损；细域开域审计在 D3 架构下 moot（域保持封闭，M3 film 审计 1.9e-14 继承）。按合同 §7 措辞保持 DIAGNOSTIC、不写 PASSED。
3. **源相位 → 已定性（清偿）**：§12.1.1 y0 扫描证明 MAP CHECK 对 y 原点**严格不变**（1.0006@+5.335° 三位小数跨全 y0 域）——+5.335° 是**真实、可复现的栈↔映射相位偏移**（40 kHz +9.3°，随离标定点增大），非 rig 原点伪影；只进入未设门的绝对相位声明（E2 比值天然免疫）；源幅值实现随之收紧至 1.0006±~3%（y0 免疫）。
4. **范围**（保留，声明性）：单频 10 kHz、法向出射（x 周期，无有限宽 directivity）、每侧幅值、compact 空气薄膜目标。声学域 c₀/nu₀/alpha₀ 为介质旋钮非空气真值。
5. M4 ≠ final production pass；Phase_5（频扫/功率/参数景观）入口决策属用户，本报告不预支。

清偿后仍保持 `PASSED_WITH_SCOPED_RISK` 措辞：#1/#4/#5 为架构与范围**声明**（不可用工作消除）；是否改判 M4 标签属用户级决策（镜像 M3 收尾先例）。

**证据链**：D3-1→core→D3-2→D3-3(one-way)→§12.1–12.4→本报告；全套 verification 148 绿（含 E2 门）；无 clipping/floor/positivity repair/prefactor tuning（合同 §10.4-6 ✓）。
