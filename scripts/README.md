# scripts/ 脚本索引

本目录是一个**扁平的 Python 命名空间包**（无 `__init__.py`）：脚本之间以 `scripts.X` 互相导入，
`tests/` 也以 `from scripts.X import ...` 复用入口，因此**所有脚本保持平铺、不分子目录**。
分类由文件名前缀 `phase1_` / `phase2_<类别>_` 承担，`ls` 下即按类别成块排序。

## 约定

- **运行方式**：在仓库根目录执行 `python -m scripts.<name>`（文档示例均用此式）。
- **命名**：`phase1_*`（参考模型）、`phase2_<类别>_*`，类别 = `m2`（验证/汇总）/ `acoustic` /
  `closure` / `robust`（鲁棒性·失败·升级）/ `phase3`（交接·Level C）；Phase_3 正式实现脚本使用 `phase3_*`，Phase_4（开边界/远场）使用 `phase4_*`。
- **共享工具枢纽**：`phase2_m2_verification.py` 导出 `load_config / sha256_file / summary_payload_digest`，
  几乎所有 `phase2_*` 诊断都从它导入；部分诊断还互相导入
  （`phase2_acoustic_attenuation_caliber`、`phase2_closure_recursive_regularized`、`phase2_acoustic_attenuation_anisotropy`）。
  这套互引正是 `scripts/` 必须扁平、改名须同步 import 的原因。
- **输出**：运行产物写入 `results/<category>/<timestamp>/`（在 `.gitignore` 中，不入库）；
  精选报告同步到 `docs/Phase_2/`（见各脚本 `--report-out` / `--out` 默认值）。
  长期留档见 `archive/M2_runs/`。

## Phase 1：参考模型与验证（`phase1_*`）

| 脚本 | 用途 |
|---|---|
| `phase1_generate_reference.py` | 生成 Phase 1 参考数据（`results/phase1_reference/*.csv`，被 manifest 哈希钉死的 frozen 夹具）。 |
| `phase1_verify_direct.py` | 不依赖 pytest 直接跑 Phase 1 验证；被 `tests/Test_phase1.py` 以模块导入。 |
| `phase1_plot_reference.py` | 生成 Phase 1 收尾图包；被 `tests/Test_phase1_plot.py` 以模块导入。 |

## Phase 2 — M2 运行与汇总（`phase2_m2_*`）

| 脚本 | 用途 |
|---|---|
| `phase2_m2_verification.py` | 跑 Phase 2 M2 验证套件 → `results/m2/<ts>/`；非 diagnostic 配置要求 pytest 与 P2-4/5/6/7/9 全过才返回 0。有效配置 digest 覆盖递归 include，summary digest 排除易变运行信息。**同时是共享工具枢纽**（见上）。 |
| `phase2_m2_summarize.py` | 汇总 `results/m2/*/summary.json` → `docs/Phase_2/M2/M2_Verification_Report.md`。 |

## Phase 2 — 声学 / 声衰减（`phase2_acoustic_*`）

| 脚本 | 用途 |
|---|---|
| `phase2_acoustic_attenuation_sweep.py` | D2Q37 声衰减闭合候选总扫描入口（trace/bulk + heat-retention，多旋钮）。 |
| `phase2_acoustic_attenuation_caliber.py` | 声衰减测量口径 + 对角过约束诊断（本征值/Prony 真值口径）。 |
| `phase2_acoustic_attenuation_anisotropy.py` | 界定已接受的对角声衰减残差（决策 A 边界）。 |
| `phase2_acoustic_bulk_viscosity.py` | 声衰减过阻尼的物理体积黏性口径复核。 |
| `phase2_acoustic_high_mode_boundary.py` | D2Q37 high-mode 声学本征支外推边界。 |
| `phase2_acoustic_high_mode_rr.py` | 残差 #3：RR 下 high-mode 声学过阻尼。 |
| `phase2_acoustic_symbol_caliber_validity.py` | 一步模态-symbol 声学口径的有效性边界（§5.5 item 1）。 |

## Phase 2 — 闭合（`phase2_closure_*`）

| 脚本 | 用途 |
|---|---|
| `phase2_closure_recursive_regularized.py` | 本地 recursive-regularized（RR）D2Q37 闭合诊断。 |
| `phase2_closure_high_order.py` | D2Q21 四阶 central-moment 闭合诊断。 |
| `phase2_closure_export_k_window.py` | 传导热流**导出 k 窗**诊断（P3-6 幅值边界机理）：逐 k 测导出比、解析谱修正乘子并反卷积 raw 形状、平衡-streaming 伪影分解（`M_art`/`M_eq0`）。输出 `results/phase2_export_window/<timestamp>/summary.json`（physics-core digest）；机理与路线判定见 `docs/Phase_2/closure/Phase2_Conductive_Export_K_Window.md`。诊断脚本，无 pass/fail 门。 |

## Phase 2 — 鲁棒性 / 敏感性 / 升级（`phase2_robust_*`）

| 脚本 | 用途 |
|---|---|
| `phase2_robust_d2q37_failure.py` | 定位 D2Q37 鲁棒性失败来源（不改生产参数）。 |
| `phase2_robust_high_mode_sensitivity.py` | high-mode 单标量闭合敏感性诊断。 |
| `phase2_robust_rr_baseline_promotion.py` | 评估把 RR（chi*）升级为默认 baseline。 |
| `phase2_robust_c3_extrapolation.py` | P2-4/5/6/7/9 的 C2+→C3 更宽外推诊断。 |
| `phase2_robust_p2_238_c3.py` | P2-2 / P2-3 / P2-8 的 C1→C3 升级诊断。 |
| `phase2_robust_transport.py` | 输运鲁棒性复核 runner → `docs/Phase_2/robustness/Phase2_Transport_Robustness_Report.md`。 |
| `phase2_robust_transport_d2q37.py` | D2Q37 专项输运鲁棒性复核 runner → `docs/Phase_2/robustness/Phase2_D2Q37_Robustness_Report.md`。 |

## Phase 2 — Phase_3 交接 / Level C 前置（`phase2_phase3_*`）

| 脚本 | 用途 |
|---|---|
| `phase2_phase3_handoff.py` | Phase_3 Level A/B 交接前风险诊断。 |
| `phase2_phase3_envelope_confirm.py` | Level C 前置：确认紧致空气工作点落在生产包络内。 |
| `phase2_phase3_qoi_scale_triage.py` | Level C 分诊：各 QoI 由哪种尺度/输运主导。 |
| `phase2_phase3_forced_near_wall_thermal.py` | Level C：受迫 10 kHz 近壁热层 sim（气侧 proxy）。 |
| `phase2_phase3_levelc_dx_recal.py` | Level C 前置：把工作点 dx 拉到标定 k。 |

## Phase 3 — 界面耦合实现（`phase3_*`）

| 脚本 | 用途 |
|---|---|
| `phase3_levela_wall_temperature.py` | P3-1 Level A prescribed wall-temperature smoke；验证底壁 no-slip + `theta_wall_lu` clamped macrostate 和复幅值相位约定，输出 `results/phase3_levela_wall_temperature/<timestamp>/`。 |
| `phase3_levelb_wall_flux.py` | P3-2 Level B prescribed wall heat-flux smoke；验证底壁 no-slip + recovered one-sided `q_g''`、能量审计和复幅值相位约定，输出 `results/phase3_levelb_wall_flux/<timestamp>/`。 |
| `phase3_levelc_coupled_10k.py` | P3-4 Level C predictor-corrector short smoke；复用 dx2p6 scoped 气侧配置、冻结热流接口、Film ODE 和 integrated energy audit，输出 `results/m3/<timestamp>/`，不声明 M3 pass。 |
| `phase3_m3_verification.py` | P3-5+ 全周期 Level C M3 验证；拟合 `T_s_hat/q_g_hat`，记录恢复出的实际壁温，并将 `wall_temperature_error_K<0.01 K` 纳入 gate。`NOT_PASSED` 以非零退出；scoped 边界状态仍可成功退出。输出 summary/report/HDF5，历史 canonical 见 M3 报告。 |
| `phase3_levela_admittance.py` | P3-6 Level A 动态热导纳（Grad 壁面、无耦合）：规定正弦壁温、row1 传导矩提取 `q_g''`（与 Level C 同口径）、末周期拟合 `Y=q_hat/T_hat` 对解析半空间参考。scratchpad 探测的提交版；输出 `results/phase3_levela_admittance/<timestamp>/`（含合同 §9 HDF5）。支持 `--frequency-hz` 诊断覆盖（标记 `frequency_overridden`）。 |
| `phase3_levelb_admittance.py` | P3-6 Level B 动态频响（Grad 类 Neumann 变体）：规定正弦单侧热流经二阶单侧差分转壁温（`theta_relax` 压 Nyquist 反馈失稳）、复用 Grad 重构，拟合 `T_wall_hat` 对 `q_hat/Y_g`；另报 `q` readback 与 `Z=T/q_readback` 一致性诊断。输出 `results/phase3_levelb_admittance/<timestamp>/`（含合同 §9 HDF5）。支持 `--frequency-hz`/`--theta-relax` 诊断覆盖。 |
| `phase3_m3_summarize.py` | 聚合 `results/m3` 与 Level A/B 导纳 run 的 summary.json 到生成型文档 `docs/Phase_3/M3/M3_Run_Summaries.md`；只读聚合，不构成判定。 |

## Phase 4 — 开边界 / 远场（`phase4_*`）

| 脚本 | 用途 |
|---|---|
| `phase4_open_boundary_reflection.py` | P4-1 开顶边界反射测量（终态）：底部**生产 thermal_grad 振荡壁温**热声源（4 个速度活塞 fixture 均被种子稳定性否决、docstring 留档），顶部 `boundary/open_cbc.py` 全条带特征阻抗边界；探针带同时谐波拟合 `p̂` 与 `v̂`，主观测量=**特征分解反射计**（`Â_inc=(p̂+Z₀v̂)/2`、`Â_ref=(p̂−Z₀v̂)/2` 逐行代数拆分，每行良态），纯压力两波 LS 降级为交叉核对（亚波长下病态：刚性盖对照读出非物理 `\|R\|=1.23`）。报告 `R_complex/R_abs`、行间散布、k 敏感性、热声发射锚，对 P4-1 门 `\|R\|<0.05`。输出 `results/m4/<timestamp>/`（summary/report + `timeseries.h5`，meta 带 M3 授权边界）。`--max-steps` 截断诊断（自动 `DIAGNOSTIC`/`NOT_CLAIMED`，防短窗假 pass）。**P4-1 终态 FAILED**：门失败根因是体积注入底板（见 `phase4_volume_injection_probe.py` 与 `docs/Phase_4/M4/P4_1_Open_Boundary_Diagnostic_Report.md`），非边界实现。导出 `characteristic_split_reflection`/`decompose_incident_reflected`/`make_thermal_drive_wall_callback`/`expected_thermoacoustic_pressure_pa` 供测试复用。 |
| `phase4_volume_injection_probe.py` | **P4-1 终态诊断探针（可复现证据链）**：三个判决实验——① 有缝本征（全周期高柱+thermal_grad 壁缝，首穿越窗口 `w⁻/w⁺` 以 ~1.1e-4/步增长）；② 算子消融（`dispersion off` 增长 ×0.25 为主导项；`acoustic_phase off`/`trace current_zero` 逐位无差）；③ 无缝对照（平滑高斯 w⁺ 脉冲全程 ≤1e-4 干净）。结论：全局周期 FFT 修正把边界缝谱泄漏转为离域波注入，有界柱稳态 `\|R\|≈0.2–0.3` 体积底板，任何局部顶边界不可击穿（合同 §13.2 降级依据）。DIAGNOSTIC-only；探针内配置开关不动生产配置。 |
| `phase4_d1_dispersion_locality_probe.py` | **D1 判决探针（局部化修正栈判死）**：刀1 消融（dispersion on/off 跑 P2-4/5/6，证明它是**热输运门必需品**——P2-5 α/热流 off→104%/61% 崩，声速/剪切不依赖）；刀2 k 空间拟合（dispersion 乘子是近阶跃高 k 平台低通，半径-n stencil=μ 的 n 次多项式无法表示高 k 平台，deg-8 残差仍 50%）。判决：FIR 局部 stencil 原理性判死。DIAGNOSTIC-only（消融开关探针内，不动冻结配置）。见立项文档 §8。 |
| `phase4_d3_coarse_acoustic_probe.py` | **D3 判决探针（粗网格声学区核心正面）**：tuned-tau（人为粘性,同比放 nu0/alpha0 保 Pr）粗网格无-dispersion 声学出射脉冲的回散射 `w⁻/w⁺`——dx334/λ104 得 **0.0018–0.0030**（比 dx2p6 注入底板 0.15 低 30×、低于门 0.05），证明**体积注入底板在声学区不存在**。同时暴露 D3 复杂度：朴素粗化 tau→0.5 变脏（需独立声学域配置）、大粘性下 heat-flux 正则化 gram 奇异（声学区应关）。DIAGNOSTIC-only。见立项文档 §9。 |
| `phase4_d3_acoustic_collision_probe.py` | **D3 简化碰撞 core 步验证探针**（读 `configs/phase4_acoustic_coarse_dx334.yaml`）：(A) 简化碰撞下开传播 backscatter 0.012<门、稳定、振幅存活 0.91；(B) **反射箱 2×2 消融**（{full,simplified}×{weak,strong filter}）。**诚实结论（修正立项 §7）**：反射能量累积的稳定性由**强局部 filter** 决定、非 heat-flux 去除——强 filter 两种碰撞都存活、弱 filter 都崩（simplified 崩得最快 @392，heat-flux 正则化本提供高 k 阻尼）；§7 见的 heat-flux gram LinAlgError 是失稳的**症状位置**、非根因。core 步真实价值=更纯声学域（不带 dx2p6 tau32 标定的 heat-flux 闭合，声学域无此物理）+ 为 D3-2 反射认证除混淆，非稳定性修法本身。导出 `run_open`/`run_closed_box`/`full_heatflux_variant`/`with_filter` 供测试复用。DIAGNOSTIC-only；冻结配置不动。 |
| `phase4_d3_reflection_probe.py` | **D3-2 开边界脉冲反射计**（读声学域配置）：`|R| = peak\|w⁻\|(反射)/peak\|w⁺\|(入射)` 于探针行（良态特征分解、非病态纯压力 LS）。三组对照闭合非退化认证——**刚性盖**（bounce-back，`\|R\|=1.26` rig 看得见反射→§7"filter 耗散反射波"退化顾虑否证）、**周期**（介质底噪 0.0075）、**sponge thickness 扫**（生产 80 行 `\|R\|=0.0004≪门 0.05`；薄→厚 `0.066→0.0004` 单调→rig 读吸收强度非固定底板）。口径：脉冲带宽代表 10 kHz（声学域无 dispersion）；刚性盖 `\|R\|>1` 是过反射对照非标定参考。导出 `run_reflection`/`make_top_rigid_lid_callback` 供测试复用。DIAGNOSTIC-only；冻结配置不动。见立项 §9。 |
| `phase4_d3_interface_probe.py` | **D3-3 界面耦合最小 fixture 判死活（DIAGNOSTIC，门未过）**：两 `GasSolver2D`（细/粗、同简化碰撞）界面耦合脉冲穿越。`run_rest_stability`（稳定阶梯 lagged→lagfree→lagfree_farend）：朴素经 monolithic `step()` 爆炸失稳（1e6×，根因 1 步时间滞后）→ **自建 lag-free stepper（同步交换 post-collision）+ 远端吸收** → 静止衰减 0.14×（稳定已解）。`run_interface_reflection` vs `run_single_baseline`：稳定后 **sharp population-patch 界面 ratio 1 反射 `\|R_iface\|=0.55`**（vs 单域基线 0.0009，透射 ~5%）≫ 门 0.05——本征阻抗失配、非伪影。**双向 G-D3-3 未过**，转单向重构（立项 §10）。慢（耦合 O(1e3–1e4) 步）；无门测试。 |
| `phase4_d3_source_extraction_probe.py` | **D3-4 第一刀：近场源提取三 rig 判定（DIAGNOSTIC）**：`run_closed_profile`（RIG1，M3 原生封闭柱冻结栈——**层内热声泵浦 1.03×u_ac**@40 kHz、T̂ 剖面教科书、封闭箱压缩模态自洽→源物理正确；早前"线性带拟合"compact-source 提取因 wrap 壁镜像双端泵浦模型失效已删）；`run_radiation_loaded(corrections_on=True)`（RIG2，冻结栈+消声 sponge——负载完美 `Z=1.005-1.009×Z₀`@−0.3° 但 `u_band=31×u_ac`@40k/**57×**@10k，**体积注入淹没物理发射→细域辐射提取判死**）；`corrections_on=False`（RIG3 归因对照，显式非冻结——31×→2.6× 坐实注入归因，对照自身退化 mass ~1e-1）。**判定：D3-4 handoff 走 compact-source 映射**（认证 T̂_s→u_src→§11 软源）。**RIG1 已升级双参数复剖面拟合**（`fit_compact_source`：`v̂(y)=u_src(1−e^{-(1+i)y/δ})+c_box` + T̂ 形状拟合，公式从 `farfield/compact_source.py` 导入→on-stack 真测生产代码；`--rigs`/`--frequency` 选择）——**10 kHz 标定点 MAP CHECK 1.0006@+5.335°**（幅值门干净过；`fit_compact_source_y0_scan`（T 剖面残差判据、反自标定）证明比值对 y 原点**严格不变**——+5.335° 为真实栈↔映射相位偏移、y0*=1.50 两频率一致、源幅值实现 ±~3%，立项 §12.1.1）；40 kHz 1.227（δ_meas/解析 1.288）= 标定点外预期。立项 §12/§12.1/§12.1.1。 |
| `phase4_m4_endtoend.py` | **P4-5/E2 端到端远场验证**：冻结链 → 两控制面共享相对 G 参考面的固定绝对观察点 → K0。R2 取三观察点幅值敏感性最大值；导数双通道幅值 `<10%` 与相位 `<10°` 均进入 verdict，失败/崩溃非零退出。2026-07-11 权威 run：E2 1.62%/0.92°、R2 2.63%、通道 −0.36%/−0.07°、SPL 86.67 dB，digest `d69bf24d881e`。 |
| `phase4_kirchhoff_verification.py` | **P4-4 K0 fixture 验证脚本**（合同 §9.2 交付件，读 `configs/phase4_kirchhoff_fixture.yaml`）：四类 manufactured fixture——圆柱波（0.08%/0.03°）、离散收敛（1.5/λ 错 164% → 12/λ 达截断底板）、平面波速度通道（0.49%/0.50°）、错核反例（104%）。导出 `run_cylindrical`/`run_plane_wave`/`run_convergence`/`run_counterexample` 供 K0 门测试复用。纯 Helmholtz、秒级、零 LBM。打印 K0 PASSED/FAILED。 |
| `phase4_d3_map_chain_smoke.py` | **D3-4(iii) 映射→注入→粗域链路 smoke（+(iv) 认证量）**：`run_chain`——`T̂_s`→compact-source 映射（**源物理用模块级 AIR 真空气常数**，不读声学 config 的介质旋钮）→`dp̂=Z₀u_src`→§11 加性软源（cos 缓升）→粗域→D3-2 顶 sponge→控制带 `ŵ⁺` 复幅读出。**加性软源固有 rig 常数 G**（辐射波=G·dp̂，FDTD 软源同款）：校准介质锁定 **G=0.1580@+152.4°**（ny512/y_s90/scale0.05/10 kHz；不得对远场回调）。门（全绿）：G1 线性 10× 偏差 0.0000/0.00°、单向 ~0.011、行波干净（相位残差 ~0、平坦 ~0.15%）。**(iv) 认证量 `c_si_over_air`**：未标定介质 +2.083% → `c0_m_s` 旋钮标定后 **+0.17%**（<2% 门 12× 余量，立项 §12.3）。范围：链路 smoke 非 M4 端到端。立项 §12.2/§12.3。 |
| `phase4_d3_oneway_probe.py` | **D3-3 单向 near→far 注入（过门）**：粗声域底部**非反射软源注入**（加性上行特征软源 + 顶/底 sponge，`_bottom_sponge`/`make_top_sponge_callback`）——细域近场单向驱动粗声域、粗域不反馈（辐射条件）。`run_injection`：单频软源→注入上行波 `w⁺` + 单向性 `w⁻/w⁺=0.009<门`；`run_bottom_reflection`：下行脉冲于注入边界，`bottom=sponge` `\|R\|=0.001`（非反射）vs `bottom=rigid` `\|R\|=0.80`（非退化对照，rig 看得见反射）。架构绕开 P4-1（远场开边界在干净粗声域、细域只经控制面 `w⁺` 提取）。导出 `run_injection`/`run_bottom_reflection` 供测试。幅值标定归 D3-4。立项 §11。 |
| `phase5_ab_smoke_digest.py` | **跨机/跨环境数值一致性 digest**：跑 G1a 合同 smoke（串行、toy、~1 min）并对 gate 行 + N=5 谐波载荷出 sha256 摘要；依赖升级后重跑作环境回归锚。**两机实测（2026-07-29）**：A 机（AMD Zen/py3.14.2）`1b9460ba3e41acec` vs B 机（Intel 12 代/py3.14.6）`32a387a3c8ec1326`——**不逐位**；归因=同版本 numpy 的运行时 SIMD 派发路径随 CPU 厂商不同（末位舍入差），唯一放大点是 α 扩展 ny=6 深衰减尾拟合（0.26%），gate 相关量全部 ≤0.01%（driven run 打印位一致、谱参考差 0.008%）。**用户确认口径（2026-07-29，D5-3）："每机逐位、跨机容差"**——权威 run 记机器指纹（provenance）、同机声明逐位复现、跨机声明容差级（gate 相关量 ≤0.01%、高 k 诊断行 ≤0.3%）、两机均可承跑权威 run。 |
| `phase5_a1_signed_zero_mean.py` | **A1 生产 runner（合同 §3.2/§15.1;WP3 P-LIN/AC1/AC2/AC3 + WP4 全阶梯)——B 机权威 `20260802T105444Z` COMPLETED**:密封 G1a 谱系 rig、fixture v2=规定 θ 符号对(G1-W 协议逐字)+**实测零均值功率归一化**(G₁=T̂₁/P̂₁,cv 修正簿记;v1 耦合驱动在无沉 rig 探针否弃留痕);ε 阶梯配置驱动(WP3 四点;**WP4=§15.1 全阶梯 7 点单 run 自含参照,配置 `a1_wp4_full_ladder`**,`unit_label` 配置驱动)+零驱空检+旧壁 DIAGNOSTIC_ONLY 对照对+1D 双分支腿(_a1_run 复用);H2_q/ε=0.4253 四位恒定、m₂=1.9987、D_G∝ε²;合法性判 verdict(生产 runner,物理数不判门)。报告 `wp3_go_nogo_decision.md` §5.1。 |
| `phase5_a2a_operating_point.py` | **A2a 工作点生产 runner(合同 §15.2;WP3 P-DC2 + WP4 地图点)——A 机权威 `20260802T104619Z` COMPLETED(Θ_DC=0.10)**:G4a 帐篷仪器逐字复用(run_tent/fit_admittance/QS-BVP 族 import,零重实现);基态+ε 增量+冷锚+耦合点;**D_OP(0.10)=−5.31% 标度律行**、DC-on-H2、χ_eff、耦合 1.0363@+1.07°;域高复验触发条件预注册(|D_OP|>10%→False)。**WP4:theta_dc/`label_tag`/`unit_label` 全配置驱动(默认逐位保持 WP3 行为),配置 `a2a_wp4_dc002`(§15.2 补全)与 `a2a_wp4_dc0075`(标度律加密,非网格点)**。报告 §5.2。 |
| `phase5_wp4_tangent_response.py` | **WP4-TAN 切线响应诊断(用户指令单元;预注册 `a5e8aea` 先于出数;B 机权威 `20260805T092726Z` COMPLETED)**:热基态 Θ_DC{0.05,0.10} 上 ε{0.00125..0.005} 三点复数 (1,ε²) 切线外推 + 逐行 1f 响应场 vs 认证谱算子(冷表/QS-1k 热表)k 谱解剖。**R1=TANGENT_CONFIRMED(−0.005/−0.007pp)=生产 D_OP 即线性化动力学响应;R2=GLOBAL_OR_LOWK_LOCALIZED=残差场无高 k 色散带特征(箱尺度/全局)**。纯分析侧新代码(切线 LSQ/场 FFT),机制测试 2 项;仪器=G4a 逐字。 |
| `phase5_wp4_jacobian_ablation.py` | **WP4-JAB 热基态 Jacobian 逐项切线消融 runner(用户授权 2026-08-08;guide `wp4_hot_basestate_jacobian_ablation_guide.md` 的执行面)**:热基态快照(run_tent snapshot 出口)→`core/tangent_step.py` 矩阵无关链式阶段 JVP(F=H∘A∘B∘S∘C;C 拆 A3/A4/A5 块)→V0–V5 硬验证(0.2pp TAN 身份门 V4)→A1–A5 单块冷态导数替换+A6 阴性对照+前二两两组合(规则冻结);Y 报告=(1,h²) 复 LSQ 外推(h 阶梯冷 smoke 选择运行 `20260808T101116Z_select_h` 冻结 {1e-4,5e-5,2.5e-5});S_i/C_i 主表+§8 冻结判读(主候选/耦合/局部敏感/`JAB_MECHANISM_NOT_CLOSED`);A1=声学相位族结构恒等 runtime assert(0 合格对角模,G2-O S6 同族独立重证)下的构造零行;h 未冻结/缺快照/无效变体 fail-loud;预注册 sha256(配置+核心+runner) 入 provenance。诊断单元(D0-7):不改 Gate/生产状态。测试 `test_phase5_wp4_jacobian_ablation.py`(14 项,A/B 双机绿)。**权威 run `20260809T195359Z` `COMPLETED`(A 机,2026-08-09/10):V0–V5 全过(V4 身份 −0.000pp)、`JAB_COUPLED_CANDIDATE_A2_A3`——A2 带重构 S=+8.8/+17.4pp 过冲、A3 宏观/平衡 S=−5.5/−10.4pp 反向、A4/A5≈0、组合两点 C=0.548/0.536 跨 50% 线且近可加(双冻结后 d_OP≈0=两块承载全部工作点响应);报告 `wp4_jacobian_ablation_report.md`**。 |
| `phase5_wp4_jab_round2.py` | **WP4-JAB2 第二轮细粒度 runner(计划 PLAN_v1.1;仪器=core/tangent_substep.py)**:四波顺序执行——锚波(A0r2 重过 V4+CTRLr2+A2ALL/A3ALL 块并集锚 vs 第一轮归档 S)→A2 五子项 σ 份额(σ≥0.5 两点同向=主项,前二组合 80% 确认线)→A3 四子项→主项对+Θ=0.075 趋势列(0.2× 闭合线);冻结分类表路由(离散边界 {A2-3,4,5}/连续物理;split_line 0.7→NSF 仲裁);V 门与 h 阶梯继承第一轮冻结值;逐例检查点沿用。诊断单元(D0-7)。测试 `test_phase5_wp4_jab_round2.py`(8 项)。**权威 run `20260810T144425Z` `COMPLETED`(阿里云 96 核抢占式,预注册 `67c97f0`):`JAB2_A2_MAIN_A2_5`(σ=1.000×2 点=整块)+`A3_DISTRIBUTED`(两族抵消)+`ROUTE_LBM_BOUNDARY`;V4r2 重锚 −1e-5pp、块锚 ±0.0000pp、0.075 第三点身份;报告 §7**。 |
| `phase5_wp4_qs1k_mechanism.py` | **QS-1k 机理判别(预注册 `32e862b` 先于出数;run `20260804T090947Z` 归档 M5_runs)**:G0 实测 α_eff(k,T) 表(逐 k 温度指数拟合)喂入认证帐篷谱算子的双比值,隔离"超出 k1 律的 k 分辨静态内容";三态判据/解释预先冻结+恒等 fail-loud+双敏感性行。**判定 `MECHANISM_NOT_CLOSED`:静态族三级同向失效,动力学残差不可约**——G4a 机理嫌疑的静态形式否证,Results I 主张加强。零 LBM 算力,纯参照计算。 |
| `phase5_wp4_results_figures.py` | **WP4 Results I/II/III 主图生成(论文轨首版)**:只读 `archive/M5_runs/` 归档(图中任何数字可溯源到权威 run),matplotlib 输出 PDF 矢量+PNG 至 `results/phase5/figures/`。图 I=D_OP 三层参照层级+残差标度律(过原点拟合 −101pp/单位);图 II=D_G∝ε² / H2 比值恒定 / m₂ 阶梯+零驱底板;图 III=χ 地图归一化传递+相位 vs 认证闭式+一致性/幅值线性(v1 实测稳定性边界带标注)。Okabe-Ito CVD 安全色板(已过 dataviz 六检,WARN 由 marker/线型/直接标注次级编码覆盖);单轴纪律。 |
| `phase5_wp4_oned_dc_arm.py` | **WP4 1D DC 臂分析腿(§15.2 参照层;WP3 §5.3 scratch 分析的脚本化)**:G3 认证 1D 仪器 lid 沉几何(H=4.61δ 帐篷匹配),A2a-lite 规定 Θ_DC 协议(mean 偏置+ε=0.005 探针),双正式分支 × Θ_DC {0.02,0.05,0.075,0.10} 全集单一约定;压缩功修正 Y 为主读出(raw 归档);WP3 scratch 对(+0.76%/+1.51%)作复现校验行,偏差>约定级则本五点系列取代 scratch 对进论文趋势线。纯 1D 分析,无 gate 声明。 |
| `phase5_nsf_hot_base_arbitration.py` | **NSF 热基态切线仲裁 runner(计划书 `NSF_hot_basestate_tangent_arbitration_plan_v1.0.md`,D0-7 诊断,2026-08-11)**:频域线性化热基态 BVP 仪器(`reference/nsf_hot_base_linear_1d.py`)上执行 {full,nograd}×Θ_DC{0,0.02,0.05,0.075,0.10}×三输运分支(计划字面 Route-B 冻结常数=主行;G3 正式 lbm-equivalent(g0) 与 physical_air=LBM 替身/验证行);判读线 V1–V7 与 U_sig=0.3pp 在热态数值前冻结于常数区;冷验证失败即中止;V6 交叉仪器=G3 时域求解器新鲜跑(常数分支)+归档 DC 臂 raw 系列(g0/physical,内嵌 provenance 常数);产出 summary.json(全阶梯+审计+§9 分类+机器指纹)。结果唯一家=`docs/Phase_5/NSF_hot_basestate_tangent_arbitration_report.md`。 |
| `phase5_wallfix_arbitration.py` | **A2-5 修复性反证 runner(用户 2026-08-11 下达;D0-7 诊断)**:v2 壁族(PROD 逐位锚/V2EQ 平衡谱形重钉扎/V2LIN/V2EQL)×{0,0.05,0.10}×h 阶梯的 JAB 切线链测量;settle 复刻对 `run_tent` 逐位锚定;判读线在 v2 热数值前冻结(PROD 身份门 0.2pp vs TAN、JAB 合法性门逐字、RESOLVED/SIGN_FLIPPED/PARTIAL/NULL/FAMILY_NULL 分类,连续参照=NSF g0 +1.18/+2.34pp);smoke=合法性+筛查(smoke 网格不复现生产符号,JAB1 +0.974% 软锚)、判决只认 auth 网格;逐例检查点(断电纪律)。结果唯一家=`docs/Phase_5/wallfix_a2a5_counterproof_report.md`。 |
| `phase5_wallfix_ghost_relax_scan.py` | **自由弛豫参数扫描 runner(2026-08-13/14,D0-7;wallfix 后续单元)**:回答"离散效应传统的标准补救=调 ghost 自由弛豫参数"审稿线。生产 `second_order` 闭合处于该参数完全正则化极限,本 runner 切到受支持的 `fourth_order` 分支重开旋钮(保留因子 1−1/τ),生产壁 PROD 全程不变(只动碰撞 ghost 扇区);阶梯双向(v2 τ>1 出数前预注册、v3 τ<1 看到 v2 后选点并诚实溯源);判读线冻结(活键 ≥1e-6/auth 锚点 0.2pp vs TAN/扫描 null <0.3pp);失稳点按 A5 χ 端点先例记档剔除(`partition_ht` 纯函数,单测覆盖);smoke→auth 两阶段、逐例检查点跨 run 复用。结果=`docs/Phase_5/wallfix_a2a5_counterproof_report.md` §8。 |
| `phase5_crossstack_collision_scan.py` | **跨栈普遍性单元 1a runner（计划书 `docs/Phase_5/crossstack_1a_plan_v1.0.md`，2026-08-14 用户指令，D0-7 诊断）**：回答"这是普遍陷阱还是你们碰撞算子的特性"审稿线——只换碰撞，壁（生产 v1.1）/几何/10 kHz/dx2p6/JAB 切线链/settle-drive 窗口全部不动。两轴一仪器：**1a-BGK**（`core/collision_bgk.py`，(τ_f,τ_g) 由冻结 mapping 导出）+ **1a-CFG**（`deviatoric_stress_policy`、`trace_bulk_policy` 及其组合 REGBGK=最接近教科书正则化 BGK 的可用闭合，外加结构惰性阳性对照 CTRL4TH）。判读线在任何变体热态数值前冻结于常数区（活键 ≥1e-6 碰撞级 + ≥1e-4 冷 |Y₀| 位移、auth 锚点 0.2pp vs TAN、分类线 1.0pp、稳定性 ρ(J)≤1+1e-6、30% |Y₀| / 2% α_eff 跨标定 caveat）；preflight 段产出谱半径稳定性图、α_eff/ν_eff 输运行（仪器对归档 G0 表锚定）与活键行；失稳/非法变体按 A5 χ 端点先例记档剔除（`partition_variants` 纯函数）；smoke→auth 两阶段、逐例检查点。结果唯一家=`docs/Phase_5/crossstack_collision_report.md`。 |
| `phase5_faceflux_wall_scan.py` | **`D1_B_BUFFER_DIAGNOSTIC` runner（2026-08-17，D0-7；非 strict-B）**：比较 PROD 与保留共享有限体积 band 的 FACEFLUX 代理（hs+1 几何）。已有 run 只执行单 h=`5e-5`，复用 stationarity/DC/r_F/V5 子集与 wallfix 数值分类；未执行完整 JAB 三档 h、odd/even、h-spread 或 direct-step identity。审计后结果=`D1_B_BUFFER_DIAGNOSTIC_NULL / D1_BUDGET_PARTIAL`，唯一家=`docs/Phase_5/faceflux_wall_report.md` REPORT_v1.1。 |
| `phase5_faceflux_strict_b_scan.py` | **D1 严格候选 B runner（2026-08-18 实施，D0-7；设计=`strict_faceflux_candidate_b_design_v1.0.md`，判读线全部冻结在常数区）**：`--mode smoke/auth/full × --stage preflight/hot/all`。preflight=设计 §5 层 3–8 的运行时部分（structural 断言、slab 壁位 N 阶梯 ≤1e-3 单调、64 周期均匀/扰动 soak + 16 周期热 DC（`A_late` 包络=预注册机器细节 3）、冷态复锚 vs 冻结 PROD 复导纳 `4.9985e-4+9.5966e-4i`（±10%/5°，失败=`STRICT_B_COLD_ILLEGAL`）、G0 admission（独立参照=`reference/strict_b_face_admission.py`；ε_g 三档 odd 配对=细节 1）、三档 full-step JVP（even 守卫=细节 2）、全量 pytest 回归），逐层 fail-loud 并落 `preflight_state_*.json`；hot=层 9（10 kHz、N=48、nx=8、Θ={0,0.05,0.10}、窗 5/4/64/2、判决 h=5e-5），**拒绝在 preflight 不绿时运行**；G0 热点仅在 admission 过后跑。比较系综=闭柱等质量（冷 1e-12/热 1e-10/Θ_DC 1%/压力 1%）；单侧导纳无因子 2；PROD 锚复用 wallfix 权威 checkpoint（零新算力）+0.2pp TAN 门；分类=设计 §6.4 五类（`STRICT_B_` 前缀、CONST_G 加 `_CONTROL`、smoke 加 `_SMOKE_SCREENING`）。逐 case checkpoint 断点续跑。结果唯一家=`docs/Phase_5/strict_b_report.md`。 |
| `phase5_a2a_strict_b.py` | **A2a-STRICT_B 原协议复测 runner（D5-9 方案 PLAN_v1.0；2026-08-19 用户授权实施+运行，判决权保留用户）**：保持 A2a 工况/读出逐字（10 kHz、N=48/nx=8、64 点/周期、settle 5/驱动 4、主窗 2–4T/替代窗 1.5–3.5T、判决对 hot 0.02/cold 0.005、hot 0.005 线性审计 ≤1e-3），只把湿节点换成 strict-B 半域镜像+G0 面导（`strict_direct_step`，单面 ledger 无 /2）；每 Θ_DC∈{0,0.02,0.05,0.075,0.10} 以 wet reference pack 的 M_wet(Θ)/A 为**精确初始列质量**独立收敛非均匀 DC 基态（单次乘性播种，之后禁调质量/压力）；合法性门=有限/逐步能量合同（1e-12 相对门与设计 §5 绝对地板 64·eps·E_total 合成）/初始质量 1e-12/全程漂移 1e-10/p̄ 对 wet 1e-2/平稳性 1e-3/DC 闭合 1e-3（地板归一）/冷锚 vs 冻结 wet 复导纳 ±10%/5°；静态参照=**新建 strict-face Robin QS-0/QS-1/QS-1k**（`reference/strict_face_robin_qs.py`，N_ref 192/384/768，U_QS=384→768 差）；预算 U_d/U_R/U_Δ/U_φ 与保守闭合率 C_R⁻ 按方案 §4 冻结；**runner 只输出 `*_CANDIDATE` 机械评估（judgement=USER_PENDING，全行 g0_scope=G0_FENCE_PENDING_USER）**。逐 case+逐周期双层 checkpoint 断点续跑（B 机外力终止史）；`--stage settle/drive/post/all`。CSV=方案 §5 schema 逐字。测试 `test_a2a_strict_b_contract.py`（12 项）。 |
| `phase5_a2a_strictb_refpack.py` | **A2a-STRICT_B wet reference pack 生成器（方案 §3.1；仅 A 机——原 run A 机谱系逐位重放，D5-3）**：四个权威 wet run（STATUS §3.1）的 f 快照未归档 → 用原 worker `run_tent(snapshot=True)` 逐位重放 4 个热 base + 1 个冷 settle（快照 flag 不触碰步进），重放 base_profile 对归档**位级校验**（回退容差 1e-12 记 fp_equal）+ 跨 run 冷例位级一致 + d_OP 距 STATUS 锚 0.2 pp 守卫；从快照提取 M_wet/A（双半域半权公式）与 p̄_wet（canonical 宏观恢复）；wet U_d 部件（错窗 1.5–3.5T/幅值对，与 strict 同一窗口拟合器；直读归档信号 → 重放偏差项=0）；R_dyn^wet 从归档 summary QS 字段转录并对方案冻结值 −2.12/−5.18/−7.61/−9.95 pp 舍入断言。输出 `archive/a2a_strict_b/`：pack json + 原始时序 npz + SHA-256 清单（含全部源文件哈希）——经 git 同步两机，strict runner 运行前验哈希。 |
| `phase5_a5_chi_map.py` | **WP4 A5 χ 地图生产 runner(合同 §15.4/§3.5;D5-6 SCOPED_GO;WP4 唯一净新增实现面)**:G4a 帐篷 rig @canonical Θ_DC=0.05,耦合仪器=G4a §A.4 认证回路逐字(run_tent coupled 路径 import,零重实现);3 锚定算例(base+冷/工作点增量)→`design_chi_point` 纯函数设计 10 点(chi_0 {0.01..3}×ε {0.01,0.05}):**χ 轴冷参照**(C_A_lu=χ₀·2\|Y_cold\|/Ω,合同 §15.4 表经 y_hs 桥精确复现;chi_eff=工作点参照逐点归档)、**P₁ 闭式反解命中目标膜幅值**(\|T̂_s\|=ε·θ0 按构造,一致性比+其 ε 依赖 D_chi=物理输出)、p1/pmean>1=有符号总功率(D0-4 数值消融口径逐点标记,沉锚 rig 无 G1b 病理族);逐点失稳守卫+eps_realized 带+0.08 包络硬守卫=合法行;material_relevance §3.5 预注册表强制齐全(缺键 fail-loud)。池纪律=G4a 已 A/B 认证 worker/调度层逐字(P-DC2 豁免先例预注册)。测试 `test_phase5_wp4_units.py`。 |
| `phase5_g4a_dc_basestate.py` | **Phase_5 G4a 稳态 DC 基态+小扰动 runner（合同 §9.1,WP2 收官门）——权威 `20260801T081856Z` `PASSED` + 耦合行重跑 `20260801T155507Z` `PASSED`**：**帐篷双带架构**（y 周期 2H_s、row0 热带+row H_s 沉带均为 v1.1 认证壁——canonical `T(H_s)=T_ambient` 的无缝跳变实现,WP1-3 判死几何的解;双柱内建重复对照）。内置参考仪器:帐篷谱参考（认证系数双点源叠加）≡ 变系数 QS-BVP（同算子 3e-14,QS-0/QS-1 操作化）+ coth 连续锚 + 解析传导种子 + **精确带能量簿记通道**（免标定;判门量全比值化）。17 算例（12 池[状态匹配 H_s 阶梯×ε 增量+冷锚+初态分支+nx 轴]+固定 P 物理分支两轮+耦合分支）。**QS 判读产出动力学非线性残差实锤**;耦合闭环=cv 重钉扎扣除+半隐式 G_inst（三层机理诊断报告 §A.4,机制映射夹具固化测试）;`--coupled-rerun-from` 单行重跑口径（supersedes provenance）。报告 `docs/Phase_5/x/dc_protocol_report.md`;测试 `test_phase5_g4a_dc_basestate.py`（6 绿）。 |
| `phase5_g2t_thermal_transfer.py` | **Phase_5 G2-T 壁温→出射声学模态热生成链 runner（合同 §7.1，WP2）——权威 run `20260730T095502Z` `PASSED`（双频）**：密封单缝 rig + v1.1 生产壁 + 规定壁温协议（G1a 口径逐字）双频 {10,20 kHz} 七行认证——T1 传递 `[Y_LBM/Y_spec(α_eff(k) 表,n=48)]/[Y_1D/Y_closed]` ≤10%/10°（**实测 +3.98%/+1.96° / +5.49%/+3.37°；20 kHz 原始差 39%/23° 被 G0 表携带因子 K=1.32@+19.2° 解释**——freeze doc §3.3「逐谐波按各自 k 有效物性」首次传递门定量兑现）、T2 出射模态（泵送剖面 û(y) 逐模连续性预测 vs 奇折叠实测控制行,最差 3.35%/5.01°）、T3 窗口、T4 域轴方向一致（差入 U_gov 5.3/6.8%）、T5 泄漏 4.4e-16、T7 卫生机器级;§7.4 H3 触发评估(未触发→豁免标签)。1D 腿=G3 认证仪器密封绝热半高匹配几何(对闭式简并 1.00003)。execute_cases 并行(10 算例 ~53 min)。报告 `docs/Phase_5/x/harmonic_transfer_report.md` §A;测试 `test_phase5_g2_harmonic_transfer.py`。 |
| `phase5_g2a_acoustic_transfer.py` | **Phase_5 G2-A 纯声学传播/读出链 runner（合同 §7.2，WP2）——权威 run `20260730T104402Z`（fixture v2）`PASSED`（双频）**：Phase_4 粗声学域 + D3-3 单向软源双频认证。**v2 夹具**（v1 权威 `20260730T095503Z` 归档为诊断 run——三处仪器操作化错误:经典 ν×100 吸收公式对简化碰撞纵模是错误物理、控制行间距≈λ@20k 相位拟合混叠、名义 z0 分解基在色散带混向）:A0 介质符号行（干净周期域一步模态本征值 P2-6 口径,按实测模态频率插值,严格夹逼不外推）+ 两段式无混叠相位拟合 + z_eff=ρ0ω/k_fit 分解基。**实测:跨度 vs 符号 0.06%/0.67%、残差 0.04°/0.08°、Â₋ 0.21%/0.17%（D3-3 真实清洁度）、c(10k)=+0.18% 判门 ✓、c(20k)=+5.67% 表征归档（围栏:仅 10 kHz 标定,不重标定）**;介质 2f 带表征=+5.7% 色散 + ~+4%/跨度空间增益（下游必携带）;compact-source 档案行 u_src(20k,α_eff(k₂))=1.427e-3 m/s（表携带因子 0.472）。报告 §B;测试同上。 |
| `phase5_g2o_operator_ablation.py` | **Phase_5 G2-O 谱修正/高波数滤波谐波消融 runner（合同 §7.3/D0-12，WP2）**：预注册变体（v0 冻结/v1 声学相位修正关/v2 滤波关/v3 滤波半/v4 dispersion 关[诊断列,碰撞内算子非 §7.3 壁后对象]）内存变异+每变体自身小幅值基线归一化(§7.3 夹具第 3 条)。**结构发现（权威 run 前机制测试）:声学相位修正族在 G2 rig 几何+冻结配置上按构造恒等**（对角支 0 合格模+高模因子 1.0 早退）→ v1 转 **S6 字节级恒等行**（可证伪,实测精确 0.0×4）。S1 单音夹具=G1-W 符号对,**settle 纪律=按箱弛豫时间 ≥~11τ_box 迁移非按周期**（τ_box 实测 1.1 周期@10k/1.47@20k;首次权威 `20260730T103844Z` 20 kHz 等周期 settle 瞬态 1.5e-7 出门,双窗衰减比 0.507=纯瞬态判别→20 kHz 覆盖 settle 20/periods 22 重跑）。S2/S3 D_G/H2 算子敏感性、S4 稳定性、S5 可归因性。报告 `docs/Phase_5/x/harmonic_operator_ablation_report.md`;测试 `test_phase5_g2_operator_ablation.py`。 |
| `phase5_g1b_levelc_amplitude.py` | **Phase_5 G1b Level C 耦合包络 runner（合同 §6.3）——判定 `FAILED`（结构性，2026-07-29）**：M3 canonical 耦合协议 + G1-W 生产壁 + 预注册回归参考（薄膜 ODE × 密封谱参考，判门行=耦合能量通道导纳 vs 谱参考[矩失准相消]）+ §23 实增益馈路标定 + detrend-1 族预注册 + execute_cases 并行。两次 B 机权威尝试暴露**密封无沉 rig 耦合回路结构性不可闭合**（矩通道 DC 反号自激/能量导数箱模自激/沉几何判死——报告 §B.1 证据链）；仪器资产与合同测试（4 绿）留存，路线决策待用户（§B.2）。 |
| `phase5_g1a_amplitude_envelope.py` | **Phase_5 G1a 幅值包络门 runner（合同 §6.2，WP2 第四门）**：G1-W 生产壁上 ε 阶梯 {0.001..0.05}+条件 {0.075,0.10}（协议逐字沿用 G1-W 权威口径→必测点确定性可比）逐行认证 §6.2 九行——能量审计=**双通道一致性漂移**（重标定冻结于 ε_min；实测随 ε 单调、0.10 处 1.41% 出包络=场形幅值依赖）、窗口敏感性、诊断细化双轴（**域轴 ny48→96**[谱参考重建，D_G 差 2.1e-4]+**dx1p3 死活探测**[mn 壁 978 步失稳实测，异常=记录不炸池]）、N=5 H2/H3 追踪。**首创进程池并行**（`execute_cases`：独立算例 --workers 默认 cpu−2、子进程 BLAS=1、装配阶梯序；串行/并行 A/B 逐位一致预验证；墙钟 8 h→1.6 h）——后续多 run gate 复用此模式。**权威 run `20260728T085824Z` PASSED+`G1A_PASSED_TO_0P05`（生产矩阵解锁至 0.075）**；报告 `docs/Phase_5/x/nonlinear_entry_gate_report.md` §A；测试 `verification/nonlinear/test_phase5_g1_amplitude_envelope.py`。 |
| `phase5_g1w_wall_neutrality.py` | **Phase_5 G1-W 热壁中性门 runner（合同 §6.1，WP2 第三门）**：冻结 dx2p6 密封 rig 上双壁矩阵（v1.1 质量中性生产候选 ε∈{0.001..0.10} + 旧壁诊断对照）逐行认证 §6.1 八行——逐步全场质量审计（0f–3f ≤1e-10，实测 1.4e-15）、导纳回归（**免标定能量通道 vs lbm-equivalent 密封谱参考**：G0 表 + run 内高 k 扩展行逐模 α_eff(k)，常 α 验证=三方闭式值；实测 +3.98%/+1.96°）、符号对边界—线性内部夹具（ε=1e-4+settle 12 纪律，≤2.8e-9）、旧壁差异审计（→DIAGNOSTIC_ONLY）+ 矩通道重标定常数归档（§23）。七文件合同 + 三态 verdict；`--smoke` 供合同测试。**权威 run `20260727T083342Z` PASSED（生产壁认证）**；首次尝试 `20260727T040121Z` 夹具行判别谱系留档报告 §3.2。报告 `docs/Phase_5/x/wall_nonlinearity_neutrality_report.md`；测试 `verification/nonlinear/test_phase5_g1w_wall_neutrality.py`。 |
| `phase5_g3_nsf1d_reference.py` | **Phase_5 G3 正式认证 runner（合同 §8，WP2 次门）**：真实空气 10 kHz 上按 §8.2 七行认证 1D NSF 参考仪器（双正式分支 g0 实测律/Sutherland）——平衡保持（生产 rig 10 周期，实测精确 0）、闭箱修正线性锚 vs Phase_1 半空间导纳（δ/12 评估：+0.0014%/−0.101°）、网格阶梯 δ/6→δ/12→δ/24（复导纳 Richardson 阶 2.00、最细两档 0.130%）、能量/质量审计（3.6e-11/6.9e-15）、Dirichlet 符号对泄漏（2.0e-10 + 物理 2f 灵敏度反证）、**密封绝热 ringdown 低耗散行**（γ 比 1.004；等温变体=无缓冲热沉诊断，双点 ∝1/dy 非退化对照）。预注册复核块：p-side 双物性 H2 消融（上界口径）+ A1 生产 settle 泄漏底板。输出合同 §16.1 七文件 + §16.2/16.3 全字段；只产 PASSED/FAILED。`--smoke` 供合同测试。**权威 run `20260726T082938Z` PASSED**；报告 `docs/Phase_5/x/nonlinear_1d_reference_report.md`；测试 `verification/nonlinear/test_phase5_g3_nsf1d.py`。 |
| `phase5_g0_effective_properties.py` | **Phase_5 G0-B 有效物性 runner（合同 §5，WP2 首门）**：冻结 dx2p6 mapping 上测 `α_eff/ν_eff/c_eff/γ_eff(T_b, k)`——4 背景温度（270–360 K）× 等压主路径 + 等密度诊断 × 双低波数层 + k1/kbox/k2/k3 生产层（复用 P2 测量内核的背景态扩展；mapping 只建一次、背景仅经初条件进入=按构造满足 §5.1）。输出 §5.4 四件套（summary/property_table/gate_evaluation/run_report）+ §5.3 门行，只产三态判定。**权威 run `20260722T173919Z`：`SCOPED_CANDIDATE`**（α/c/γ@k1 过、ν+低波数 scoped）；冻结文档 `docs/Phase_5/x/nonlinear_model_freeze.md`。配置 `configs/phase5/g0_effective_properties/`；测试 `verification/nonlinear/test_phase5_g0_effective_properties.py`。 |
